<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Scofieldyu</title>
  
  <subtitle>高筑墙-广积粮-缓称王</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://Scofieldsu.github.io/"/>
  <updated>2026-09-06T03:44:51.439Z</updated>
  <id>https://Scofieldsu.github.io/</id>
  
  <author>
    <name>俞 元</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>多环境管理：dev/staging/prod 的测试环境治理</title>
    <link href="https://Scofieldsu.github.io/2026/07/10/%E5%A4%9A%E7%8E%AF%E5%A2%83%E7%AE%A1%E7%90%86%EF%BC%9Adev/staging/prod%E7%9A%84%E6%B5%8B%E8%AF%95%E7%8E%AF%E5%A2%83%E6%B2%BB%E7%90%86/"/>
    <id>https://Scofieldsu.github.io/2026/07/10/多环境管理：dev/staging/prod的测试环境治理/</id>
    <published>2026-07-10T19:50:00.000Z</published>
    <updated>2026-09-06T03:44:51.439Z</updated>
    
    <content type="html"><![CDATA[<p>“这个环境是谁动过？”“staging 怎么和 prod 不一样？”“我本地好的，怎么一上环境就挂？”——环境问题是团队协作里最阴性的成本：它不产生 bug 单，但消耗每个人每天 30 分钟的排查时间。2026 年把多环境管理做成了一套显式机制，这篇讲四个核心动作。</p><h2 id="一-环境不是一台服务器是一份声明"><a class="markdownIt-Anchor" href="#一-环境不是一台服务器是一份声明"></a> 一、环境不是&quot;一台服务器&quot;，是&quot;一份声明&quot;</h2><p>混乱的根源是把环境理解成&quot;那台机器&quot;。正确模型：<strong>环境 = 配置声明 + 数据声明 + 依赖声明</strong>，三者全部版本化：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># envs/staging.yaml（进 git，变更走 MR）</span></span><br><span class="line"><span class="attr">name:</span> <span class="string">staging</span></span><br><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">git_ref:</span> <span class="string">release-2026.07</span>        <span class="comment"># 部署的代码版本（由发布系统写入）</span></span><br><span class="line">  <span class="attr">image:</span> <span class="string">registry/app:20260710-a3f9c21</span></span><br><span class="line"><span class="attr">config:</span></span><br><span class="line">  <span class="attr">base:</span> <span class="string">shared.yaml</span>               <span class="comment"># 基础配置（端口、超时、日志级别）</span></span><br><span class="line">  <span class="attr">overrides:</span></span><br><span class="line">    <span class="attr">ws_mock:</span> <span class="string">enabled</span>              <span class="comment"># staging 特有：Mock WS 供帧</span></span><br><span class="line">    <span class="attr">data_pool:</span> <span class="string">staging_shared</span>     <span class="comment"># 数据池声明</span></span><br><span class="line"><span class="attr">dependencies:</span>                     <span class="comment"># 依赖环境（声明式，启动时校验）</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">user-service</span></span><br><span class="line">    <span class="attr">version:</span> <span class="number">2026.07</span><span class="string">-rc2</span></span><br><span class="line">    <span class="attr">health:</span> <span class="string">/healthz</span></span><br><span class="line"><span class="attr">data:</span></span><br><span class="line">  <span class="attr">seed_version:</span> <span class="string">seed-2026.07.03</span>   <span class="comment"># 数据快照版本（见&quot;数据工厂&quot;那篇）</span></span><br><span class="line">  <span class="attr">refresh:</span> <span class="string">weekly</span>                 <span class="comment"># 每周一从 prod 脱敏同步</span></span><br></pre></td></tr></table></figure><p><strong>核心收益</strong>：环境状态从&quot;登录机器看&quot;变成&quot;读一个文件&quot;。两个环境的行为差异 = 两份 yaml 的 diff，<code>git blame</code> 直接回答&quot;谁在什么时候改了什么&quot;。</p><h2 id="二-环境指纹每个测试报告必须携带"><a class="markdownIt-Anchor" href="#二-环境指纹每个测试报告必须携带"></a> 二、环境指纹：每个测试报告必须携带</h2><p>&quot;我本地好的，环境上挂&quot;的终结方案：<strong>测试运行时采集环境指纹，写进每一份报告</strong>：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">&quot;env&quot;</span>: <span class="string">&quot;staging&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;fingerprint&quot;</span>: &#123;</span><br><span class="line">    <span class="attr">&quot;app_sha&quot;</span>: <span class="string">&quot;a3f9c21&quot;</span>,</span><br><span class="line">    <span class="attr">&quot;config_hash&quot;</span>: <span class="string">&quot;9c2e17ab&quot;</span>,        <span class="comment">// envs/staging.yaml + shared.yaml 的 hash</span></span><br><span class="line">    <span class="attr">&quot;seed_version&quot;</span>: <span class="string">&quot;seed-2026.07.03&quot;</span>,</span><br><span class="line">    <span class="attr">&quot;deps&quot;</span>: &#123; <span class="attr">&quot;user-service&quot;</span>: <span class="string">&quot;2026.07-rc2&quot;</span> &#125;,</span><br><span class="line">    <span class="attr">&quot;collected_at&quot;</span>: <span class="string">&quot;2026-07-10T19:50:00+08:00&quot;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>排查时第一动作不是&quot;复现&quot;，而是<strong>对比指纹</strong>：</p><ul><li>失败 run 的 <code>config_hash</code> ≠ 预期 → 有人改了环境配置（blame 配置 MR）；</li><li><code>seed_version</code> 落后 → 数据漂移（该周刷新没执行）；</li><li>某个 dep 的 version 和计划不符 → 依赖被单独部署了。</li></ul><p><strong>指纹把&quot;环境问题排查&quot;从 2 小时玄学变成 5 分钟 diff</strong>。指纹采集是 pytest 的 session 级 fixture，一行 <code>env_fingerprint.collect()</code> 的事。</p><h2 id="三-环境漂移检测每天对账不等出事"><a class="markdownIt-Anchor" href="#三-环境漂移检测每天对账不等出事"></a> 三、环境漂移检测：每天对账，不等出事</h2><p>配置会&quot;漂移&quot;：有人登录机器手改了 Nginx、直接 <code>kubectl set env</code> 覆盖了声明值。漂移检测 = <strong>声明 vs 实际</strong>每日对账：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 每日 08:00 跑（CI job）</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">drift_check</span>(<span class="params">env</span>):</span></span><br><span class="line">    declared = load_env_yaml(env)</span><br><span class="line">    actual = probe_actual(env)       <span class="comment"># 拉取：镜像 sha / 配置项 / 依赖版本 / 数据版本号</span></span><br><span class="line">    diffs = []</span><br><span class="line">    <span class="keyword">if</span> declared.deploy.image != actual.image:</span><br><span class="line">        diffs.append(<span class="string">&quot;image 漂移：声明 %s 实际 %s&quot;</span> % ...)</span><br><span class="line">    <span class="keyword">for</span> k <span class="keyword">in</span> declared.config.overrides:</span><br><span class="line">        <span class="keyword">if</span> declared.config.overrides[k] != actual.config.get(k):</span><br><span class="line">            diffs.append(<span class="string">f&quot;config <span class="subst">&#123;k&#125;</span> 漂移&quot;</span>)</span><br><span class="line">    <span class="keyword">if</span> diffs:</span><br><span class="line">        notify_drift(env, diffs)     <span class="comment"># 告警 + 自动回滚工单</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span></span><br></pre></td></tr></table></figure><p><strong>对账失败的两条出路</strong>：要么&quot;声明错了&quot;（补 MR 修正声明），要么&quot;实际错了&quot;（回滚机器）。两条路都强制收敛回&quot;声明即事实&quot;——<strong>漂移检测的价值不在报警，在于逼着每次变更都过 git</strong>。</p><h2 id="四-环境的生命周期谁建-谁养-谁拆"><a class="markdownIt-Anchor" href="#四-环境的生命周期谁建-谁养-谁拆"></a> 四、环境的生命周期：谁建、谁养、谁拆</h2><p>环境资源（尤其 staging）最常见的浪费是&quot;僵尸环境&quot;：项目结束了环境还在跑，占了 8C16G 三个月。生命周期四问写进平台：</p><table><thead><tr><th>问题</th><th>机制</th></tr></thead><tbody><tr><td>谁建？</td><td>环境申请走平台（填声明 yaml），自动开资源 + 写权限</td></tr><tr><td>谁养？</td><td>声明里的 owner 字段，漂移告警/哨兵失败 @ owner</td></tr><tr><td>谁拆？</td><td><strong>闲置检测</strong>：连续 14 天无测试 run + 无登录 → 冻结（关流量保数据）；再 14 天 → 销毁（数据先备份 30 天）</td></tr><tr><td>例外？</td><td>长稳专用环境打标 <code>exempt: soak</code>，按长稳节奏豁免闲置检测</td></tr></tbody></table><p><strong>冻结比销毁重要</strong>：环境销毁后重建要 2 小时（数据、证书、网络白名单），冻结（保留资源、关入口）成本是 0。&quot;先冻后销&quot;把误杀成本降到最低——真要用，一键解冻。</p><h2 id="五-一个容易忽略的点环境的语义要统一"><a class="markdownIt-Anchor" href="#五-一个容易忽略的点环境的语义要统一"></a> 五、一个容易忽略的点：环境的&quot;语义&quot;要统一</h2><p>最后一条最软但最值钱：<strong>每个环境能测什么，要全员共识</strong>。我们的约定：</p><ul><li><code>dev</code>：单服务联调，数据随便造，不保证依赖完整性；</li><li><code>staging</code>：全链路 + Mock 外部依赖（WS/支付/短信全走 Mock），<strong>功能结论的合法出处</strong>；</li><li><code>prod-canary</code>：只读流量 + 金丝雀，<strong>性能/容量结论的合法出处</strong>；</li><li><code>prod</code>：<strong>任何测试动作需审批</strong>，只允许白名单的只读探针。</li></ul><p>约定落在平台里：CI profile 声明目标环境，<code>dev</code> 环境的&quot;全链路回归&quot;跑不出来（依赖声明校验不过）——<strong>用机制固化语义</strong>，比培训十遍都管用。</p><h2 id="六-小结"><a class="markdownIt-Anchor" href="#六-小结"></a> 六、小结</h2><p>环境治理四个动作：<strong>声明化</strong>（环境 = 版本化的 yaml，不是机器）、<strong>指纹</strong>（每份报告带环境指纹，排查变 diff）、<strong>对账</strong>（每日漂移检测，逼变更过 git）、<strong>生命周期</strong>（owner + 闲置冻结销毁）。环境问题的本质是&quot;状态不可见&quot;，四个动作都在把状态变成可见、可追溯、可问责的东西。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;“这个环境是谁动过？”“staging 怎么和 prod 不一样？”“我本地好的，怎么一上环境就挂？”——环境问题是团队协作里最阴性的成本：它不产生 bug 单，但消耗每个人每天 30 分钟的排查时间。2026 年把多环境管理做成了一套显式机制，这篇讲四个核心动作。&lt;/p&gt;
      
    
    </summary>
    
      <category term="测试工程" scheme="https://Scofieldsu.github.io/categories/%E6%B5%8B%E8%AF%95%E5%B7%A5%E7%A8%8B/"/>
    
    
      <category term="工程化" scheme="https://Scofieldsu.github.io/tags/%E5%B7%A5%E7%A8%8B%E5%8C%96/"/>
    
      <category term="环境管理" scheme="https://Scofieldsu.github.io/tags/%E7%8E%AF%E5%A2%83%E7%AE%A1%E7%90%86/"/>
    
      <category term="CI" scheme="https://Scofieldsu.github.io/tags/CI/"/>
    
  </entry>
  
  <entry>
    <title>7×24 长稳测试：让系统在时间里暴露问题</title>
    <link href="https://Scofieldsu.github.io/2026/04/22/7x24%E9%95%BF%E7%A8%B3%E6%B5%8B%E8%AF%95%EF%BC%9A%E8%AE%A9%E7%B3%BB%E7%BB%9F%E5%9C%A8%E6%97%B6%E9%97%B4%E9%87%8C%E6%9A%B4%E9%9C%B2%E9%97%AE%E9%A2%98/"/>
    <id>https://Scofieldsu.github.io/2026/04/22/7x24长稳测试：让系统在时间里暴露问题/</id>
    <published>2026-04-22T20:20:00.000Z</published>
    <updated>2026-09-06T03:44:51.437Z</updated>
    
    <content type="html"><![CDATA[<p>功能测试验证&quot;能不能用&quot;，长稳（soak）测试验证&quot;能不能一直用&quot;——内存泄漏、连接池耗尽、句柄泄漏、缓存膨胀，这些 bug 的触发条件是<strong>时间</strong>，跑 30 分钟的回归永远抓不到。2026 年把 7×24 长稳做成了常态化基建，这篇讲设计。</p><h2 id="一-长稳-把回归跑-24-小时"><a class="markdownIt-Anchor" href="#一-长稳-把回归跑-24-小时"></a> 一、长稳 ≠ “把回归跑 24 小时”</h2><p>最常见的误用：把回归套件循环跑 24 小时，看最后有没有挂。这只能抓到&quot;累计执行量&quot;类问题，抓不到<strong>状态漂移</strong>类问题——长稳的核心观察对象是<strong>系统随时间的指标曲线</strong>，不是用例通过率：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># soak/plan.yaml</span></span><br><span class="line"><span class="attr">name:</span> <span class="string">customer-service-soak</span></span><br><span class="line"><span class="attr">duration:</span> <span class="string">72h</span></span><br><span class="line"><span class="attr">target:</span> <span class="string">staging-full</span></span><br><span class="line"><span class="attr">traffic:</span>                       <span class="comment"># 流量模型：按线上比例混合</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">profile:</span> <span class="string">chat_mixed</span>        <span class="comment"># 会话 + 消息 + 搜索（见 Locust 那篇）</span></span><br><span class="line">    <span class="attr">weight:</span> <span class="number">70</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">profile:</span> <span class="string">ws_event_stream</span>   <span class="comment"># 长连接事件流（Mock WS 服务供帧）</span></span><br><span class="line">    <span class="attr">weight:</span> <span class="number">20</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">profile:</span> <span class="string">heavy_upload</span>      <span class="comment"># 大文件/富媒体（每 4h 一波）</span></span><br><span class="line">    <span class="attr">weight:</span> <span class="number">10</span></span><br><span class="line"><span class="attr">observe:</span>                       <span class="comment"># 观察项：这才是长稳的主体</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">memory_heap:</span> &#123; <span class="attr">threshold:</span> <span class="string">&quot;growth &gt; 300MB / 12h&quot;</span> &#125;</span><br><span class="line">  <span class="bullet">-</span> <span class="attr">memory_rss:</span>   &#123; <span class="attr">threshold:</span> <span class="string">&quot;abs &gt; 8GB&quot;</span> &#125;</span><br><span class="line">  <span class="bullet">-</span> <span class="attr">goroutine_fd:</span> &#123; <span class="attr">threshold:</span> <span class="string">&quot;growth &gt; 500 / 12h&quot;</span> &#125;</span><br><span class="line">  <span class="bullet">-</span> <span class="attr">p99_latency:</span>  &#123; <span class="attr">threshold:</span> <span class="string">&quot;drift &gt; 50% vs baseline&quot;</span> &#125;</span><br><span class="line">  <span class="bullet">-</span> <span class="attr">ws_reconnect:</span> &#123; <span class="attr">threshold:</span> <span class="string">&quot;rate &gt; 0.1%/min&quot;</span> &#125;</span><br><span class="line">  <span class="bullet">-</span> <span class="attr">disk_logs:</span>    &#123; <span class="attr">threshold:</span> <span class="string">&quot;abs &gt; 50GB&quot;</span> &#125;</span><br></pre></td></tr></table></figure><p><strong>用例只是背景流量</strong>，让系统&quot;活着干活&quot;；观察项的阈值告警才是长稳的产出。</p><h2 id="二-三个必须预埋的探针"><a class="markdownIt-Anchor" href="#二-三个必须预埋的探针"></a> 二、三个必须预埋的探针</h2><p>长稳的&quot;能看见&quot;靠三个探针，全部在长稳开始前部署：</p><h3 id="1-资源采样器每-60s"><a class="markdownIt-Anchor" href="#1-资源采样器每-60s"></a> 1. 资源采样器（每 60s）</h3><p>进程级（heap/RSS/fd/线程数）+ 依赖级（DB 连接池占用、Redis 内存、消息队列深度）+ 系统级（CPU/IO/磁盘）。<strong>基线是第 0~2 小时的稳态值</strong>，不是历史值——同一套系统换个部署配置，历史基线就没意义。</p><h3 id="2-泄漏判定看斜率不看绝对值"><a class="markdownIt-Anchor" href="#2-泄漏判定看斜率不看绝对值"></a> 2. 泄漏判定：看&quot;斜率&quot;不看&quot;绝对值&quot;</h3><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">leak_verdict</span>(<span class="params">samples, window_hours=<span class="number">12</span></span>):</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    内存/句柄曲线做线性回归：</span></span><br><span class="line"><span class="string">    - 斜率 ≈ 0：平稳（GC 正常回收）</span></span><br><span class="line"><span class="string">    - 斜率 &gt; 阈值：泄漏（持续只涨不跌）</span></span><br><span class="line"><span class="string">    - 锯齿但峰值下移：健康（周期性回收在变快，偶发正常）</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    slope = linear_regression_slope(samples[-window_hours*<span class="number">60</span>:])</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;leak&quot;</span> <span class="keyword">if</span> slope &gt; LEAK_SLOPE_THRESHOLD <span class="keyword">else</span> <span class="string">&quot;ok&quot;</span></span><br></pre></td></tr></table></figure><p><strong>锯齿曲线要单独处理</strong>：Java/Go 的 GC 曲线天然锯齿，直接对&quot;当前值&quot;设阈值会天天误报；对<strong>峰值序列</strong>做回归（峰值也在涨 = 真泄漏）才是对的判定。</p><h3 id="3-功能哨兵每小时"><a class="markdownIt-Anchor" href="#3-功能哨兵每小时"></a> 3. 功能哨兵（每小时）</h3><p>长稳期间系统可能在&quot;资源还健康&quot;之前就已经<strong>功能悄悄坏了</strong>（如 WS 消息延迟从 200ms 漂到 3s，资源曲线看不出）。每小时跑一组 5 分钟的功能哨兵用例（登录/发消息/下单核心路径），<strong>哨兵失败 = 立即终止长稳并保留现场</strong>，比 72 小时后才发现&quot;中间 40 小时都是坏的&quot;便宜得多。</p><h2 id="三-现场保留长稳的价值-80-在崩溃后"><a class="markdownIt-Anchor" href="#三-现场保留长稳的价值-80-在崩溃后"></a> 三、现场保留：长稳的价值 80% 在崩溃后</h2><p>长稳崩了（或判定泄漏）的那一刻，现场数据比什么都重要，必须<strong>预埋自动保留</strong>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">on_soak_alert</span>(<span class="params">alert</span>):</span></span><br><span class="line">    <span class="comment"># 1) 抓进程现场：heap dump（Java）/ py-spy dump（Python）/ fd 全表</span></span><br><span class="line">    <span class="comment"># 2) 抓依赖现场：DB slow log 最近 1h / 连接池快照 / MQ 深度</span></span><br><span class="line">    <span class="comment"># 3) 抓流量现场：最近 10min 的流量回放包（复现用）</span></span><br><span class="line">    <span class="comment"># 4) 全量打包 var/soak/&lt;run_id&gt;/crash/ 并推送告警群（附一句话判定）</span></span><br><span class="line">    capture_all(alert.run_id)</span><br><span class="line">    notify(<span class="string">f&quot;长稳 <span class="subst">&#123;alert.metric&#125;</span> 异常，现场已保留：<span class="subst">&#123;url&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure><p><strong>流量回放包</strong>是被低估的宝贝：泄漏/崩溃的复现往往需要&quot;特定流量序列&quot;，保留最近 10 分钟的完整流量，本地重放就能在 30 分钟内复现，不用等下次长稳（72 小时后）。</p><h2 id="四-常态化运行成本与节奏"><a class="markdownIt-Anchor" href="#四-常态化运行成本与节奏"></a> 四、常态化运行：成本与节奏</h2><table><thead><tr><th>项</th><th>策略</th></tr></thead><tbody><tr><td>频率</td><td>每周一次 72h（覆盖一个完整的工作周期 + 周末流量形态）</td></tr><tr><td>环境</td><td>staging 独立实例（不占生产、不和功能测试互扰）</td></tr><tr><td>成本</td><td>一台 8C16G + 一台流量发生器，月成本约 = 半个人日</td></tr><tr><td>终止条件</td><td>观察项告警 / 功能哨兵失败 / 72h 自然结束</td></tr><tr><td>产出</td><td>每周一份长稳周报（斜率曲线 + 哨兵结果 + 告警明细），进发版评审</td></tr></tbody></table><p><strong>关键纪律</strong>：长稳告警必须有人认领。第一次长稳揪出泄漏后，修复验证要<strong>再跑一次定向短长稳（12h，放大该流量维度）</strong>，确认斜率归零才算闭环——&quot;修了&quot;和&quot;修好了&quot;在泄漏问题上是两回事。</p><h2 id="五-实测战绩"><a class="markdownIt-Anchor" href="#五-实测战绩"></a> 五、实测战绩</h2><p>落地三个月抓到三个功能测试完全无感的问题：</p><ol><li><strong>WS 连接句柄泄漏</strong>：长连接重连后旧 fd 不释放，72h 后 fd 增长 8000+，线上表现为&quot;服务跑两周后偶发断连&quot;——正是用户工单里那个&quot;时好时坏&quot;；</li><li><strong>日志异步队列膨胀</strong>：日志级别配置漂移导致异步队列只进不出，内存 6h 涨 2GB，锯齿回归判定精准锁定；</li><li><strong>搜索缓存雪崩</strong>：缓存过期策略在&quot;凌晨低流量 + 批量预热&quot;组合下触发，只有 72h 覆盖凌晨+早高峰的完整长稳能撞上。</li></ol><h2 id="六-小结"><a class="markdownIt-Anchor" href="#六-小结"></a> 六、小结</h2><p>长稳测试的三个支柱：<strong>流量是背景，指标斜率是主体</strong>（看回归不看绝对值）、<strong>功能哨兵保功能真相</strong>（资源健康 ≠ 功能健康）、<strong>现场自动保留</strong>（崩溃那一刻的数据决定问题能否复现）。长稳不是&quot;跑更久的回归&quot;，是<strong>用时间换 bug 维度</strong>——功能、性能、泄漏，各测各的，谁也不能代替谁。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;功能测试验证&amp;quot;能不能用&amp;quot;，长稳（soak）测试验证&amp;quot;能不能一直用&amp;quot;——内存泄漏、连接池耗尽、句柄泄漏、缓存膨胀，这些 bug 的触发条件是&lt;strong&gt;时间&lt;/strong&gt;，跑 30 分钟的回归永远抓不到。2026 年把 7×24
      
    
    </summary>
    
      <category term="稳定性测试" scheme="https://Scofieldsu.github.io/categories/%E7%A8%B3%E5%AE%9A%E6%80%A7%E6%B5%8B%E8%AF%95/"/>
    
    
      <category term="长稳测试" scheme="https://Scofieldsu.github.io/tags/%E9%95%BF%E7%A8%B3%E6%B5%8B%E8%AF%95/"/>
    
      <category term="泄漏检测" scheme="https://Scofieldsu.github.io/tags/%E6%B3%84%E6%BC%8F%E6%A3%80%E6%B5%8B/"/>
    
      <category term="稳定性" scheme="https://Scofieldsu.github.io/tags/%E7%A8%B3%E5%AE%9A%E6%80%A7/"/>
    
  </entry>
  
  <entry>
    <title>测试报告体系：从&quot;一张 HTML&quot;到&quot;决策看板&quot;</title>
    <link href="https://Scofieldsu.github.io/2026/01/18/%E6%B5%8B%E8%AF%95%E6%8A%A5%E5%91%8A%E4%BD%93%E7%B3%BB%EF%BC%9A%E4%BB%8E%E4%B8%80%E5%BC%A0HTML%E5%88%B0%E5%86%B3%E7%AD%96%E7%9C%8B%E6%9D%BF/"/>
    <id>https://Scofieldsu.github.io/2026/01/18/测试报告体系：从一张HTML到决策看板/</id>
    <published>2026-01-18T20:00:00.000Z</published>
    <updated>2026-09-06T03:44:51.439Z</updated>
    
    <content type="html"><![CDATA[<p>测试报告做到第五年，最大的变化是<strong>受众变了</strong>：早期报告是&quot;给测试自己看的记录&quot;，现在是&quot;给研发定位问题、给 PM 判断发版、给管理层看趋势&quot;的<strong>决策工具</strong>。同一份数据，三种受众要三种视角。这篇讲报告体系的分层设计。</p><h2 id="一-三层视角l1-结论-l2-定位-l3-追溯"><a class="markdownIt-Anchor" href="#一-三层视角l1-结论-l2-定位-l3-追溯"></a> 一、三层视角：L1 结论、L2 定位、L3 追溯</h2><p>一份报告同时服务三种人，结构必须分层，而不是把所有信息堆在一个页面：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">L1 结论层（PM/管理层，10 秒看完）</span><br><span class="line">├── 发版建议：GO / NO-GO / RISK-GO（显式结论，不是&quot;请看详情&quot;）</span><br><span class="line">├── 关键指标卡：通过率、新增失败、阻塞数、回归趋势 7 日曲线</span><br><span class="line">└── 一句话摘要：自动生成（&quot;本次 612 用例，失败 3 个均为订单模块</span><br><span class="line">    已知问题 #4821，无新增阻塞，建议发版&quot;）</span><br><span class="line"></span><br><span class="line">L2 定位层（研发，5 分钟定位）</span><br><span class="line">├── 失败用例列表：按模块聚合，同类失败折叠</span><br><span class="line">├── 每个失败：断言差异 diff + 失败时截图/控件树 + 相关日志 30 行</span><br><span class="line">└── 环境指纹：git sha、数据版本、设备/机型、配置 hash</span><br><span class="line"></span><br><span class="line">L3 追溯层（测试/审计，按需深挖）</span><br><span class="line">├── 全量用例明细（含通过的）</span><br><span class="line">├── 执行时序图：每用例开始/结束时间、重叠关系、排队等待</span><br><span class="line">└── 原始产物索引：日志全文、录屏、请求响应 dump（按 run_id 归档）</span><br></pre></td></tr></table></figure><p><strong>最容易被忽略的是 L1 的&quot;显式结论&quot;</strong>。报告页面上写 600+ 行明细，但没人愿意替读者下结论——&quot;能不能发版&quot;是报告存在的理由。结论生成规则要写死：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">conclude</span>(<span class="params">run, history</span>):</span></span><br><span class="line">    blockers = [c <span class="keyword">for</span> c <span class="keyword">in</span> run.failed <span class="keyword">if</span> c.severity == <span class="string">&quot;blocker&quot;</span>]</span><br><span class="line">    <span class="keyword">if</span> blockers:</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;NO-GO&quot;</span>, <span class="string">f&quot;<span class="subst">&#123;<span class="built_in">len</span>(blockers)&#125;</span> 个阻塞级失败&quot;</span></span><br><span class="line">    known = [c <span class="keyword">for</span> c <span class="keyword">in</span> run.failed <span class="keyword">if</span> c.linked_issue]</span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">len</span>(known) == <span class="built_in">len</span>(run.failed):</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;RISK-GO&quot;</span>, <span class="string">&quot;失败均为已知问题&quot;</span></span><br><span class="line">    <span class="keyword">if</span> run.pass_rate &gt;= history.p75_pass_rate:</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;GO&quot;</span>, <span class="string">&quot;通过率处于 7 日正常区间&quot;</span></span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;RISK-GO&quot;</span>, <span class="string">&quot;通过率低于近 7 日 P75，建议人工确认&quot;</span></span><br></pre></td></tr></table></figure><p>结论规则可审计（页面上展示&quot;结论依据&quot;），<strong>避免&quot;报告说全绿但没人敢发&quot;的信用损耗</strong>。</p><h2 id="二-失败聚合把-40-个失败变成-3-个问题"><a class="markdownIt-Anchor" href="#二-失败聚合把-40-个失败变成-3-个问题"></a> 二、失败聚合：把 40 个失败变成 3 个问题</h2><p>研发最痛的不是&quot;40 个用例失败&quot;，而是&quot;40 个失败其实是 1 个根因&quot;。聚合算法按<strong>失败指纹</strong>归组：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">fingerprint</span>(<span class="params">case, failure</span>):</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;失败指纹：模块 + 失败阶段 + 首个错误特征&quot;&quot;&quot;</span></span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">        case.module,                       <span class="comment"># 订单模块</span></span><br><span class="line">        failure.phase,                     <span class="comment"># 支付步骤</span></span><br><span class="line">        normalize(failure.first_error),    <span class="comment"># &quot;timeout waiting btn_pay visible&quot;</span></span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line"><span class="comment"># 归组后展示：</span></span><br><span class="line"><span class="comment"># [订单·支付] 37 个用例：等待支付按钮超时 → 关联 #4821（按钮文案改版）</span></span><br><span class="line"><span class="comment"># [消息·列表] 2 个用例：会话数断言 5 != 3 → 数据工厂配置未同步</span></span><br><span class="line"><span class="comment"># [环境] 1 个用例：设备 A-03 触摸无响应 → 设备故障（非产品问题）</span></span><br></pre></td></tr></table></figure><p>归组后每组的&quot;代表用例&quot;只展开一个详情，其余折叠——<strong>研发按组修，不按用例修</strong>。指纹里的 <code>normalize</code> 很关键：把具体值抹掉（“等待 btn_X 超时” → “等待按钮超时”），否则同一根因会裂成 N 组。</p><h2 id="三-趋势单份报告没意义曲线才有"><a class="markdownIt-Anchor" href="#三-趋势单份报告没意义曲线才有"></a> 三、趋势：单份报告没意义，曲线才有</h2><p>单份报告回答&quot;这次好不好&quot;，趋势回答&quot;在变好还是变坏&quot;：</p><ul><li><strong>通过率曲线</strong>：7/30/90 日，标注每次发版竖线——&quot;这个版本之后通过率掉了 5%&quot;比&quot;本次通过率 92%&quot;有信息量十倍；</li><li><strong>修复时长（MTTR）</strong>：失败用例从&quot;首次失败&quot;到&quot;首次转绿&quot;的天数，按模块统计——某个模块 MTTR 从 2 天涨到 9 天，是&quot;该模块测试债在积累&quot;的先行指标；</li><li><strong>flaky 率</strong>：同一用例同环境&quot;绿红交替&quot;的比例，&gt;5% 的用例进 flaky 清单强制治理（修稳定或隔离），<strong>flaky 用例是报告可信度的毒药</strong>——团队一旦相信&quot;那个用例本来就爱挂&quot;，所有报告都失去信用。</li></ul><h2 id="四-报告的工程实现"><a class="markdownIt-Anchor" href="#四-报告的工程实现"></a> 四、报告的工程实现</h2><p>Flask 侧三个设计：</p><ol><li><strong>报告是纯静态产物</strong>：每次 run 结束，<code>report_builder</code> 把 L1/L2/L3 渲染成独立 HTML（数据内联，单文件可离线打开），落 <code>var/results/&lt;run_id&gt;/report.html</code>——<strong>不依赖服务端在线</strong>，链接发给任何人都能看；</li><li><strong>数据与呈现分离</strong>：先落 <code>result.json</code>（结构化全量数据），HTML 只是它的视图——同一份 json 可以再生成钉钉摘要、Excel 明细、CI 门禁判定；</li><li><strong>增量生成</strong>：用例逐例上报时<strong>增量更新</strong> report.html（L2 失败列表实时可见），不用等全量跑完——3 小时的回归，第一个阻塞失败出现时研发就能开始看，不用等 3 小时后&quot;报告出炉&quot;。</li></ol><h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2><p>报告体系的核心是<strong>受众分层</strong>（L1 结论/L2 定位/L3 追溯）、<strong>失败聚合</strong>（按指纹归组，研发按组修）、<strong>趋势指标</strong>（通过率曲线 + MTTR + flaky 率）。报告的价值不在&quot;好看&quot;，在于<strong>每个层的人都 10 秒内拿到自己要的东西</strong>——PM 拿到 GO/NO-GO，研发拿到 diff 和截图，审计拿到全量明细。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;测试报告做到第五年，最大的变化是&lt;strong&gt;受众变了&lt;/strong&gt;：早期报告是&amp;quot;给测试自己看的记录&amp;quot;，现在是&amp;quot;给研发定位问题、给 PM 判断发版、给管理层看趋势&amp;quot;的&lt;strong&gt;决策工具&lt;/strong&gt;。同一份数据，三种受
      
    
    </summary>
    
      <category term="测试工程" scheme="https://Scofieldsu.github.io/categories/%E6%B5%8B%E8%AF%95%E5%B7%A5%E7%A8%8B/"/>
    
    
      <category term="工程化" scheme="https://Scofieldsu.github.io/tags/%E5%B7%A5%E7%A8%8B%E5%8C%96/"/>
    
      <category term="测试报告" scheme="https://Scofieldsu.github.io/tags/%E6%B5%8B%E8%AF%95%E6%8A%A5%E5%91%8A/"/>
    
      <category term="数据可视化" scheme="https://Scofieldsu.github.io/tags/%E6%95%B0%E6%8D%AE%E5%8F%AF%E8%A7%86%E5%8C%96/"/>
    
  </entry>
  
  <entry>
    <title>SSE 实时推流：测试平台日志与状态推送的完整实践</title>
    <link href="https://Scofieldsu.github.io/2025/11/06/SSE%E5%AE%9E%E6%97%B6%E6%8E%A8%E6%B5%81%EF%BC%9A%E6%B5%8B%E8%AF%95%E5%B9%B3%E5%8F%B0%E6%97%A5%E5%BF%97%E4%B8%8E%E7%8A%B6%E6%80%81%E6%8E%A8%E9%80%81%E7%9A%84%E5%AE%8C%E6%95%B4%E5%AE%9E%E8%B7%B5/"/>
    <id>https://Scofieldsu.github.io/2025/11/06/SSE实时推流：测试平台日志与状态推送的完整实践/</id>
    <published>2025-11-06T20:40:00.000Z</published>
    <updated>2026-09-06T03:44:51.437Z</updated>
    
    <content type="html"><![CDATA[<p>测试平台的实时性需求很朴素：任务在跑，浏览器要<strong>现在</strong>看到日志和进度。技术选型时 WebSocket 是惯性答案，但我们的场景（单向推送 + HTTP 基建 + 内网环境）用 SSE 更简单更稳。这篇是 Flask + SSE 的完整实践：从事件设计到断线重连到性能边界。</p><h2 id="一-为什么选-sse-而不是-websocket"><a class="markdownIt-Anchor" href="#一-为什么选-sse-而不是-websocket"></a> 一、为什么选 SSE 而不是 WebSocket</h2><table><thead><tr><th>维度</th><th>SSE</th><th>WebSocket</th></tr></thead><tbody><tr><td>方向</td><td>单向（服务端→客户端）✅ 正好</td><td>双向（用不上）</td></tr><tr><td>断线重连</td><td><strong>浏览器原生</strong>（<code>EventSource</code> 自动带 <code>Last-Event-ID</code>）</td><td>自己写</td></tr><tr><td>基建穿透</td><td>普通 HTTP，代理/网关/鉴权全复用</td><td>部分内网设备要开 ws 端口</td></tr><tr><td>流量开销</td><td>每帧带 HTTP 头（~200B）</td><td>2~14B</td></tr><tr><td>连接数</td><td>受 HTTP 限制（同域 6 连接）</td><td>无此限制</td></tr></tbody></table><p>我们的场景：日志推送、任务状态、SSE 事件流全是<strong>单向</strong>；同域 6 连接上限通过<strong>事件合并到 1~2 个流</strong>解决；200B 帧头对日志场景无感。<strong>省掉的&quot;重连 + 握手 + 基建&quot;复杂度，比帧头钱值钱得多</strong>。</p><h2 id="二-事件设计一个流多频道"><a class="markdownIt-Anchor" href="#二-事件设计一个流多频道"></a> 二、事件设计：一个流，多频道</h2><p>不要每个功能开一个 SSE 端点（吃连接数），合并成<strong>一个事件流 + 类型字段</strong>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@bp.route(<span class="params"><span class="string">&quot;/ui/events&quot;</span></span>)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">events</span>():</span></span><br><span class="line">    sub = live_log.subscribe()          <span class="comment"># 拿到专属队列</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">stream</span>():</span></span><br><span class="line">        last_ping = time.time()</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">                <span class="keyword">try</span>:</span><br><span class="line">                    ev = sub.get(timeout=<span class="number">15</span>)</span><br><span class="line">                <span class="keyword">except</span> queue.Empty:</span><br><span class="line">                    <span class="keyword">yield</span> <span class="string">&quot;: ping\n\n&quot;</span>          <span class="comment"># 心跳：保活 + 检测死连接</span></span><br><span class="line">                    last_ping = time.time()</span><br><span class="line">                    <span class="keyword">continue</span></span><br><span class="line">                <span class="keyword">if</span> ev <span class="keyword">is</span> SENTINEL:             <span class="comment"># 服务端通知关闭</span></span><br><span class="line">                    <span class="keyword">break</span></span><br><span class="line">                <span class="comment"># SSE 帧格式：event 类型 + id（断线重连续传）+ data</span></span><br><span class="line">                <span class="keyword">yield</span> <span class="string">f&quot;id: <span class="subst">&#123;ev.seq&#125;</span>\nevent: <span class="subst">&#123;ev.<span class="built_in">type</span>&#125;</span>\ndata: <span class="subst">&#123;ev.payload&#125;</span>\n\n&quot;</span></span><br><span class="line">        <span class="keyword">finally</span>:</span><br><span class="line">            live_log.unsubscribe(sub)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> Response(stream(), mimetype=<span class="string">&quot;text/event-stream&quot;</span>,</span><br><span class="line">                    headers=&#123;<span class="string">&quot;Cache-Control&quot;</span>: <span class="string">&quot;no-cache&quot;</span>,</span><br><span class="line">                             <span class="string">&quot;X-Accel-Buffering&quot;</span>: <span class="string">&quot;no&quot;</span>&#125;)   <span class="comment"># 关 nginx 缓冲！</span></span><br></pre></td></tr></table></figure><p>三个关键细节：</p><ol><li><strong><code>id: seq</code> 单调递增</strong>：断线重连时浏览器自动带 <code>Last-Event-ID</code>，服务端从该 seq 续传——环形缓冲保留最近 2000 帧即可覆盖&quot;断网 30 秒&quot;的场景；</li><li><strong>15s 心跳</strong>：内网代理普遍 30~60s 空闲断连，心跳必须比它短；心跳同时是死连接检测器——<code>yield</code> 给死连接会抛 BrokenPipe，finally 里退订，<strong>不泄漏订阅队列</strong>；</li><li><strong><code>X-Accel-Buffering: no</code></strong>：nginx 默认缓冲 SSE 响应，日志会&quot;攒一批才出来&quot;——这个头不加，所有实时性都白搭（我们第一个版本就栽在这，表现为&quot;日志有 5 秒延迟&quot;）。</li></ol><p>客户端：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> es = <span class="keyword">new</span> EventSource(<span class="string">&quot;/api/ui/events&quot;</span>);</span><br><span class="line">es.addEventListener(<span class="string">&quot;log&quot;</span>, <span class="function">(<span class="params">e</span>) =&gt;</span> appendLog(<span class="built_in">JSON</span>.parse(e.data)));</span><br><span class="line">es.addEventListener(<span class="string">&quot;task_state&quot;</span>, <span class="function">(<span class="params">e</span>) =&gt;</span> updateBadge(<span class="built_in">JSON</span>.parse(e.data)));</span><br><span class="line"><span class="comment">// 重连由浏览器原生处理，Last-Event-ID 自动带上，服务端续传</span></span><br></pre></td></tr></table></figure><h2 id="三-发布侧进程内注册表-背压"><a class="markdownIt-Anchor" href="#三-发布侧进程内注册表-背压"></a> 三、发布侧：进程内注册表 + 背压</h2><p>Flask 单进程内，发布者是执行引擎的后台线程：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">LiveLogRegistry</span>:</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;任务 ID → 订阅者队列列表；发布 O(订阅者数)，慢订阅者不阻塞发布&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">emit</span>(<span class="params">self, task_id, <span class="built_in">type</span>, payload</span>):</span></span><br><span class="line">        seq = self._seq.<span class="built_in">next</span>()</span><br><span class="line">        self._ring.append((task_id, seq, <span class="built_in">type</span>, payload))   <span class="comment"># 环形缓冲，断线续传</span></span><br><span class="line">        <span class="keyword">for</span> q <span class="keyword">in</span> self._subs.get(task_id, []):</span><br><span class="line">            <span class="keyword">if</span> q.full():</span><br><span class="line">                <span class="comment"># 慢订阅者：丢弃旧帧 + 发一条 &quot;dropped:N&quot; 告知客户端跳帧</span></span><br><span class="line">                q.get_nowait()</span><br><span class="line">                q.put((task_id, seq, <span class="built_in">type</span>, payload))</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                q.put((task_id, seq, <span class="built_in">type</span>, payload))</span><br></pre></td></tr></table></figure><p><strong>背压策略：发布永不阻塞</strong>。执行引擎是生产核心，一个卡住的浏览器连接不能拖慢任务执行；代价是慢订阅者丢帧——丢帧通过 seq 缺口暴露（客户端发现 <code>id</code> 跳号，拉一次 <code>/ui/events/backfill?from=seq</code> 补全），<strong>实时性让位于完整性兜底</strong>。</p><h2 id="四-性能边界与实测"><a class="markdownIt-Anchor" href="#四-性能边界与实测"></a> 四、性能边界与实测</h2><p>单台 Flask 机器（4C8G）实测：</p><table><thead><tr><th>并发 SSE 连接</th><th>日志吞吐（发布侧）</th><th>事件循环影响</th></tr></thead><tbody><tr><td>50</td><td>500 条/s</td><td>无感</td></tr><tr><td>200</td><td>800 条/s</td><td>发布线程 CPU +3%</td></tr><tr><td>500</td><td>1000 条/s（帧合并后）</td><td>需要帧合并（见下）</td></tr></tbody></table><p><strong>帧合并</strong>是 500 连接档的关键：日志高频时不逐条 yield，而是 100ms 窗口内的多条日志合并成一帧（<code>data: [ev1, ev2, ev3]</code>）——浏览器端本来也是批量渲染的，逐条推是浪费。合并窗口 100ms 对&quot;看日志&quot;场景的实时性无感知。</p><p><strong>Gunicorn 注意</strong>：SSE 长连接会占死 worker。生产配置 <code>--threads</code> 或 <code>gevent worker</code>，<strong>SSE 端点单独限流</strong>（内网工具场景，单用户 2 个连接足够）。</p><h2 id="五-踩过的坑"><a class="markdownIt-Anchor" href="#五-踩过的坑"></a> 五、踩过的坑</h2><ol><li><strong>浏览器自动重连风暴</strong>：服务端重启瞬间，200 个 <code>EventSource</code> 同时重连 + 补拉，日志接口被打满——重连加<strong>客户端随机抖动</strong>（<code>setTimeout(random 0~3s)</code> 后再 new EventSource），补拉接口限流；</li><li><strong><code>yield</code> 里的异常吞掉</strong>：generator 里 BrokenPipe 不捕获，订阅队列泄漏，表现为&quot;越跑内存越高&quot;——finally 退订是硬要求；</li><li><strong>时区/换行</strong>：日志 payload 里的 <code>\n</code> 必须转义成 <code>\\n</code>——SSE 协议里裸 <code>\n</code> 是帧分隔符，多行日志直接破坏帧结构（这个 bug 排查了一下午）。</li></ol><h2 id="六-小结"><a class="markdownIt-Anchor" href="#六-小结"></a> 六、小结</h2><p>SSE 实践的四个支柱：<strong>单流多频道</strong>（省连接数）、<strong>seq + 心跳 + 缓冲续传</strong>（断线无损）、<strong>发布不阻塞 + seq 缺口补全</strong>（背压与完整性兼顾）、<strong>帧合并</strong>（规模档性能）。WebSocket 不是更好的答案——它是更贵、更复杂、我们场景里不需要的答案。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;测试平台的实时性需求很朴素：任务在跑，浏览器要&lt;strong&gt;现在&lt;/strong&gt;看到日志和进度。技术选型时 WebSocket 是惯性答案，但我们的场景（单向推送 + HTTP 基建 + 内网环境）用 SSE 更简单更稳。这篇是 Flask + SSE 的完整实践：从事
      
    
    </summary>
    
      <category term="后端开发" scheme="https://Scofieldsu.github.io/categories/%E5%90%8E%E7%AB%AF%E5%BC%80%E5%8F%91/"/>
    
    
      <category term="Flask" scheme="https://Scofieldsu.github.io/tags/Flask/"/>
    
      <category term="SSE" scheme="https://Scofieldsu.github.io/tags/SSE/"/>
    
      <category term="实时推送" scheme="https://Scofieldsu.github.io/tags/%E5%AE%9E%E6%97%B6%E6%8E%A8%E9%80%81/"/>
    
  </entry>
  
  <entry>
    <title>Python 异步并发在测试基础设施里的正确用法</title>
    <link href="https://Scofieldsu.github.io/2025/08/10/Python%E5%BC%82%E6%AD%A5%E5%B9%B6%E5%8F%91%E5%9C%A8%E6%B5%8B%E8%AF%95%E5%9F%BA%E7%A1%80%E8%AE%BE%E6%96%BD%E9%87%8C%E7%9A%84%E6%AD%A3%E7%A1%AE%E7%94%A8%E6%B3%95/"/>
    <id>https://Scofieldsu.github.io/2025/08/10/Python异步并发在测试基础设施里的正确用法/</id>
    <published>2025-08-10T20:10:00.000Z</published>
    <updated>2026-09-06T03:44:51.437Z</updated>
    
    <content type="html"><![CDATA[<p>asyncio 最大的误用不是写错语法，而是<strong>用错地方</strong>：把&quot;IO 密集&quot;和&quot;CPU 密集&quot;混在一个事件循环里，或者在同步测试框架里硬塞 async。2025 年把测试基础设施（Mock WS 服务、设备调度、SSE 推送）整体迁到 asyncio，这篇总结哪些地方该异步、哪些地方坚持同步。</p><h2 id="一-判断标准你的瓶颈在等待吗"><a class="markdownIt-Anchor" href="#一-判断标准你的瓶颈在等待吗"></a> 一、判断标准：你的瓶颈在等待吗</h2><p>异步化的收益公式：<strong>并发数 × 等待时间占比</strong>。测试基础设施里各组件的画像：</p><table><thead><tr><th>组件</th><th>主要等待</th><th>该不该 asyncio</th></tr></thead><tbody><tr><td>Mock WS 服务（50 连接收发）</td><td>网络 IO，99% 在等</td><td>✅ 核心场景</td></tr><tr><td>设备心跳聚合（50 台 × 10s）</td><td>网络 IO</td><td>✅</td></tr><tr><td>用例结果回收（大量小 HTTP）</td><td>网络 IO</td><td>✅</td></tr><tr><td>任务调度决策（装箱算法）</td><td>CPU，微秒级</td><td>❌ 纯同步函数</td></tr><tr><td>报告生成（HTML 渲染）</td><td>CPU + 少量文件 IO</td><td>❌ 同步或丢线程池</td></tr><tr><td>本地日志落盘</td><td>文件 IO，本地</td><td>❌ 同步写（本地盘够快）</td></tr></tbody></table><p><strong>经验法则</strong>：一个组件里超过 80% 的时间在等网络，才值得异步化；否则同步代码 + 线程池更简单可靠。Mock WS 服务是典型该异步的：同步 socket 方案下 50 个连接要 50 个线程，每线程一个 select 循环，内存和切换成本都难看；asyncio 下一个事件循环全搞定。</p><h2 id="二-事件循环里的三个纪律"><a class="markdownIt-Anchor" href="#二-事件循环里的三个纪律"></a> 二、事件循环里的三个纪律</h2><h3 id="1-禁止在协程里跑阻塞调用"><a class="markdownIt-Anchor" href="#1-禁止在协程里跑阻塞调用"></a> 1. 禁止在协程里跑阻塞调用</h3><p>这是 asyncio 头号事故来源：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># ❌ 灾难：整个事件循环卡 2 秒，50 个连接全断</span></span><br><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">def</span> <span class="title">handle_msg</span>(<span class="params">conn</span>):</span></span><br><span class="line">    result = heavy_compute(payload)      <span class="comment"># 阻塞！</span></span><br><span class="line">    <span class="keyword">await</span> conn.send(result)</span><br><span class="line"></span><br><span class="line"><span class="comment"># ✅ CPU 活丢线程池</span></span><br><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">def</span> <span class="title">handle_msg</span>(<span class="params">conn</span>):</span></span><br><span class="line">    result = <span class="keyword">await</span> asyncio.to_thread(heavy_compute, payload)</span><br><span class="line">    <span class="keyword">await</span> conn.send(result)</span><br></pre></td></tr></table></figure><p>团队约定：<strong>协程里只允许 <code>await</code> 和纯计算（微秒级）</strong>。code review 时看到协程里出现 <code>time.sleep</code> / <code>requests.post</code> / 大循环，直接打回。</p><h3 id="2-任务生命周期要可取消"><a class="markdownIt-Anchor" href="#2-任务生命周期要可取消"></a> 2. 任务生命周期要可取消</h3><p>Mock 服务里&quot;广播 500 个连接&quot;这种 fan-out，取消语义必须正确：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">def</span> <span class="title">broadcast</span>(<span class="params">self, payload: <span class="built_in">dict</span></span>):</span></span><br><span class="line">    tasks = [conn.send_json(payload) <span class="keyword">for</span> conn <span class="keyword">in</span> self.clients.values()]</span><br><span class="line">    <span class="comment"># gather 返回即全部完成/失败；要中断时用 TaskGroup（3.11+）</span></span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">with</span> asyncio.TaskGroup() <span class="keyword">as</span> tg:</span><br><span class="line">        <span class="keyword">for</span> t <span class="keyword">in</span> tasks:</span><br><span class="line">            tg.create_task(t)</span><br><span class="line">    <span class="comment"># 客户端断开 → 对应 task 抛 ConnectionClosed，TaskGroup 自动取消兄弟任务</span></span><br></pre></td></tr></table></figure><p><code>TaskGroup</code> 的价值在<strong>级联取消</strong>：一个慢客户端卡住，不影响其他客户端的完成；取消整个 broadcast 时，所有 in-flight 的 send 一起收掉。手写 <code>gather</code> + try/except 的取消逻辑，我们吃过&quot;取消后还有一半 send 在飞&quot;的亏。</p><h3 id="3-背压要显式"><a class="markdownIt-Anchor" href="#3-背压要显式"></a> 3. 背压要显式</h3><p>设备心跳聚合、SSE 推送都是&quot;生产快于消费&quot;的场景。无界队列 = 内存无限涨：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HeartbeatAggregator</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="comment"># 有界队列 + 显式丢弃策略（心跳只认最新值，旧的直接丢）</span></span><br><span class="line">        self.q = asyncio.Queue(maxsize=<span class="number">1000</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">async</span> <span class="function"><span class="keyword">def</span> <span class="title">push</span>(<span class="params">self, hb</span>):</span></span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            self.q.put_nowait(hb)</span><br><span class="line">        <span class="keyword">except</span> asyncio.QueueFull:</span><br><span class="line">            self.dropped += <span class="number">1</span>        <span class="comment"># 计数进监控，不阻塞生产方</span></span><br></pre></td></tr></table></figure><p><strong>丢弃策略必须显式声明</strong>（心跳丢旧、结果不可丢），而不是&quot;队列满了才想办法&quot;。测试结果回收这类不可丢的数据，用&quot;满了就 await put（背压到上游）&quot;，上游慢就整体变慢——<strong>宁可慢，不能丢</strong>。</p><h2 id="三-和-pytest-的边界"><a class="markdownIt-Anchor" href="#三-和-pytest-的边界"></a> 三、和 pytest 的边界</h2><p>测试框架本身（pytest）是同步的，async 组件怎么测？边界清晰化：</p><ol><li><strong>异步组件的单元测试</strong>：<code>pytest-asyncio</code>，每个测试一个干净的事件循环（<code>event_loop</code> fixture function 级）；</li><li><strong>集成测试</strong>：异步服务跑在独立进程，pytest 走 HTTP/WS 客户端测它——<strong>跨进程边界就是天然的同步/异步隔离带</strong>，测试代码不需要 async；</li><li><strong>禁止</strong>：在 pytest 的 fixture 里 <code>loop.run_until_complete</code> 手动管事件循环——99% 的&quot;async 测试偶发挂&quot;来自 fixture 里的事件循环泄漏。</li></ol><h2 id="四-可观测性async-代码出事时最难查"><a class="markdownIt-Anchor" href="#四-可观测性async-代码出事时最难查"></a> 四、可观测性：async 代码出事时最难查</h2><p>同步代码的 traceback 是完整调用栈，async 代码的&quot;现场&quot;散落在 N 个协程里：</p><ul><li><code>asyncio.all_tasks()</code> + <code>task.print_stack()</code> 写进 <code>/debug/dump</code> 端点——服务卡住时 dump 全部协程栈，比猜快十倍；</li><li>每个长任务（&gt;1s 的协程）进耗时看板：<code>await</code> 前后打点，&quot;等待耗时 vs 计算耗时&quot;分开统计——<strong>P99 劣化先看等待还是计算</strong>，方向就对了；</li><li>结构化日志带 <code>task_id</code>：一个业务帧的生命周期（接收→处理→下发）全部可串联。</li></ul><h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2><p>asyncio 在测试基础设施里的正确姿势：<strong>IO 密集组件（Mock WS、心跳聚合、结果回收）用 asyncio，CPU 活和同步框架坚持原样</strong>；事件循环三纪律（不阻塞、可取消、显式背压）；测试边界跨进程隔离。异步不是架构信仰，是&quot;等待占比 80%&quot;这个数字的函数。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;asyncio 最大的误用不是写错语法，而是&lt;strong&gt;用错地方&lt;/strong&gt;：把&amp;quot;IO 密集&amp;quot;和&amp;quot;CPU 密集&amp;quot;混在一个事件循环里，或者在同步测试框架里硬塞 async。2025 年把测试基础设施（Mock WS 服务、设备
      
    
    </summary>
    
      <category term="后端开发" scheme="https://Scofieldsu.github.io/categories/%E5%90%8E%E7%AB%AF%E5%BC%80%E5%8F%91/"/>
    
    
      <category term="Python" scheme="https://Scofieldsu.github.io/tags/Python/"/>
    
      <category term="asyncio" scheme="https://Scofieldsu.github.io/tags/asyncio/"/>
    
      <category term="并发" scheme="https://Scofieldsu.github.io/tags/%E5%B9%B6%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>接口自动化平台设计：从&quot;跑脚本&quot;到&quot;用例资产&quot;</title>
    <link href="https://Scofieldsu.github.io/2025/05/15/%E6%8E%A5%E5%8F%A3%E8%87%AA%E5%8A%A8%E5%8C%96%E5%B9%B3%E5%8F%B0%E8%AE%BE%E8%AE%A1%EF%BC%9A%E4%BB%8E%E8%B7%91%E8%84%9A%E6%9C%AC%E5%88%B0%E7%94%A8%E4%BE%8B%E8%B5%84%E4%BA%A7/"/>
    <id>https://Scofieldsu.github.io/2025/05/15/接口自动化平台设计：从跑脚本到用例资产/</id>
    <published>2025-05-15T20:30:00.000Z</published>
    <updated>2026-09-06T03:44:51.439Z</updated>
    
    <content type="html"><![CDATA[<p>接口自动化做到第三年，最大的反思是：<strong>脚本数量从 200 涨到 2000 之后，&quot;能跑&quot;和&quot;可信&quot;是两回事</strong>。200 个脚本的时候&quot;全绿&quot;就是好信号；2000 个的时候，全绿可能只是&quot;没人敢改&quot;。这篇讲接口平台的几个核心设计：让用例成为可度量的资产。</p><h2 id="一-用例模型步骤化-依赖声明"><a class="markdownIt-Anchor" href="#一-用例模型步骤化-依赖声明"></a> 一、用例模型：步骤化 + 依赖声明</h2><p>自由编码的脚本（pytest 直接写）灵活但不可分析。平台把用例收敛成<strong>结构化步骤</strong>：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># cases/order/test_pay_flow.yaml</span></span><br><span class="line"><span class="attr">case:</span> <span class="string">order_pay_flow</span></span><br><span class="line"><span class="attr">depends_on:</span>            <span class="comment"># 数据依赖：平台按拓扑序准备</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">data:order_with_items</span></span><br><span class="line"><span class="attr">steps:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">create</span></span><br><span class="line">    <span class="attr">api:</span> <span class="string">POST</span> <span class="string">/api/orders</span></span><br><span class="line">    <span class="attr">expect:</span> &#123; <span class="attr">status:</span> <span class="number">201</span>, <span class="attr">&quot;body.id&quot;:</span> <span class="string">&quot;$oid&quot;</span> &#125;      <span class="comment"># 断言 + 变量捕获</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">pay</span></span><br><span class="line">    <span class="attr">api:</span> <span class="string">POST</span> <span class="string">/api/orders/$oid/pay</span></span><br><span class="line">    <span class="attr">params:</span> &#123; <span class="attr">amount:</span> <span class="string">&quot;$.order.amount&quot;</span> &#125;            <span class="comment"># 引用前置数据</span></span><br><span class="line">    <span class="attr">expect:</span> &#123; <span class="attr">status:</span> <span class="number">200</span>, <span class="attr">&quot;body.status&quot;:</span> <span class="string">&quot;PAID&quot;</span> &#125;</span><br><span class="line">  <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">verify_stock</span></span><br><span class="line">    <span class="attr">api:</span> <span class="string">GET</span> <span class="string">/api/orders/$oid</span></span><br><span class="line">    <span class="attr">expect:</span> &#123; <span class="attr">&quot;body.stock_decremented&quot;:</span> <span class="literal">true</span> &#125;</span><br><span class="line"><span class="attr">teardown:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">api:</span> <span class="string">POST</span> <span class="string">/api/orders/$oid/cancel</span></span><br></pre></td></tr></table></figure><p>结构化换来三件事：</p><ul><li><strong>可分析</strong>：平台知道每个用例&quot;调了哪些接口、依赖哪些数据、断言了什么&quot;——覆盖率、脆弱度、依赖图全是数据；</li><li><strong>可重放单步</strong>：<code>pay</code> 步骤挂了，可以只重放 <code>pay</code> 及其后续，不用从 <code>create</code> 开始；</li><li><strong>可迁移</strong>：用例是 YAML 不是代码，接口域名/环境切换是配置行为。</li></ul><p>自由编码没有消失——<strong>探索性、一次性验证</strong>仍写脚本，验证通过的价值逻辑沉淀进结构化用例。</p><h2 id="二-断言分级契约断言-vs-快照断言"><a class="markdownIt-Anchor" href="#二-断言分级契约断言-vs-快照断言"></a> 二、断言分级：契约断言 vs 快照断言</h2><p>断言写太严（全字段快照）→ 接口任何变更都挂，团队学会&quot;改接口先改断言&quot;，断言失去意义；写太松（只看 status）→ 断言形同虚设。两级断言体系：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">expect:</span></span><br><span class="line">  <span class="attr">status:</span> <span class="number">200</span>                          <span class="comment"># L1 契约断言：稳定字段，永不挂</span></span><br><span class="line">  <span class="attr">body:</span></span><br><span class="line">    <span class="attr">status:</span> <span class="string">&quot;PAID&quot;</span>                     <span class="comment"># L1</span></span><br><span class="line">    <span class="attr">paid_at:</span> &#123; <span class="attr">type:</span> <span class="string">&quot;datetime&quot;</span> &#125;      <span class="comment"># L1 类型断言</span></span><br><span class="line">  <span class="attr">snapshot:</span>                            <span class="comment"># L2 快照断言：全字段，允许受控漂移</span></span><br><span class="line">    <span class="attr">ignore:</span> [<span class="string">&quot;body.trace_id&quot;</span>, <span class="string">&quot;body.server_time&quot;</span>]</span><br><span class="line">    <span class="attr">drift_policy:</span> <span class="string">warn</span>                 <span class="comment"># 漂移先告警不失败，人工确认</span></span><br></pre></td></tr></table></figure><p><strong>L2 快照的 <code>drift_policy</code> 是关键</strong>：接口加了个新字段、改了个字段顺序，快照会&quot;漂移&quot;。直接失败 = 噪音；直接忽略 = 失去意义。<code>warn</code> 策略把漂移变成<strong>待审清单</strong>：每周 10 分钟过一遍漂移告警，确认&quot;预期变更&quot;转基线，&quot;意外变更&quot;开 issue。<strong>断言从&quot;门禁&quot;变成&quot;变更雷达&quot;</strong>，这是接口自动化最有价值也最被低估的功能。</p><h2 id="三-覆盖率接口自动化第一次有北极星"><a class="markdownIt-Anchor" href="#三-覆盖率接口自动化第一次有北极星"></a> 三、覆盖率：接口自动化第一次有北极星</h2><p>结构化用例让覆盖率可计算：</p><ul><li><strong>接口覆盖</strong>：路由 × 方法 × 参数组合（有接口的 OpenAPI 规格就能算）；</li><li><strong>断言覆盖</strong>：每个响应字段的&quot;被断言次数&quot;——<code>server_time</code> 从来没被断言过，说明没人关注它；</li><li><strong>错误路径覆盖</strong>：4xx/5xx 分支有没有用例打进去——大多数团队的错误路径覆盖是 0，而线上事故 80% 出在错误路径。</li></ul><p>看板上的&quot;接口覆盖率 92%&quot;是虚荣指标，<strong>“错误路径覆盖率 12% → 45%”</strong> 才是真指标。平台把错误路径用例单独标记（<code>negative: true</code>），CI 的 smoke 档必跑全部 negative 用例——错误路径的用例通常最便宜（不需要完整数据准备）也最值钱。</p><h2 id="四-环境数据接口语义的-fixture"><a class="markdownIt-Anchor" href="#四-环境数据接口语义的-fixture"></a> 四、环境数据：接口语义的 fixture</h2><p>接口用例的数据准备比 UI 简单（不用造&quot;看得见&quot;的东西），但有自己的坑：<strong>状态机依赖</strong>。&quot;支付&quot;用例需要一个&quot;已创建未支付&quot;的订单，直接调 create 接口造——但 create 接口本身挂了怎么办？</p><p>解法是<strong>数据 fixture 分层</strong>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># fixtures/order.py</span></span><br><span class="line"><span class="meta">@pytest.fixture(<span class="params">scope=<span class="string">&quot;session&quot;</span></span>)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">order_factory</span>(<span class="params">api</span>):</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;订单工厂：create + 预检（create 挂了在这里 fail，用例层不用关心）&quot;&quot;&quot;</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">make</span>(<span class="params">**kw</span>):</span></span><br><span class="line">        resp = api.post(<span class="string">&quot;/api/orders&quot;</span>, json=kw)</span><br><span class="line">        <span class="keyword">assert</span> resp.status == <span class="number">201</span>, <span class="string">f&quot;订单工厂失效: <span class="subst">&#123;resp&#125;</span>&quot;</span></span><br><span class="line">        <span class="keyword">return</span> Order(resp.json())</span><br><span class="line">    <span class="keyword">return</span> make</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test_pay</span>(<span class="params">order_factory</span>):</span></span><br><span class="line">    order = order_factory(item_id=<span class="string">&quot;sku-001&quot;</span>)   <span class="comment"># 数据问题在 fixture 层暴露</span></span><br><span class="line">    ...</span><br></pre></td></tr></table></figure><p><strong>原则</strong>：fixture 是&quot;数据能力的唯一入口&quot;，fixture 挂了 = 环境/数据层问题（归因清晰），用例挂了 = 被测接口问题。归因清晰了，&quot;环境不好&quot;和&quot;产品有 bug&quot;不再混战。</p><h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2><p>接口平台从&quot;脚本堆&quot;到&quot;用例资产&quot;的四个设计：<strong>结构化用例</strong>（可分析、可单步重放）、<strong>两级断言</strong>（契约 + 快照漂移雷达）、<strong>三维覆盖率</strong>（接口/断言/错误路径）、<strong>fixture 归因</strong>（数据问题与产品问题分家）。脚本数量到千级之后，平台的价值不再是&quot;跑得快&quot;，而是&quot;每个绿的/红的都有解释&quot;。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;接口自动化做到第三年，最大的反思是：&lt;strong&gt;脚本数量从 200 涨到 2000 之后，&amp;quot;能跑&amp;quot;和&amp;quot;可信&amp;quot;是两回事&lt;/strong&gt;。200 个脚本的时候&amp;quot;全绿&amp;quot;就是好信号；2000 个的时候，全绿可能只是&amp;
      
    
    </summary>
    
      <category term="接口测试" scheme="https://Scofieldsu.github.io/categories/%E6%8E%A5%E5%8F%A3%E6%B5%8B%E8%AF%95/"/>
    
    
      <category term="Python" scheme="https://Scofieldsu.github.io/tags/Python/"/>
    
      <category term="接口测试" scheme="https://Scofieldsu.github.io/tags/%E6%8E%A5%E5%8F%A3%E6%B5%8B%E8%AF%95/"/>
    
      <category term="平台设计" scheme="https://Scofieldsu.github.io/tags/%E5%B9%B3%E5%8F%B0%E8%AE%BE%E8%AE%A1/"/>
    
  </entry>
  
  <entry>
    <title>手机设备农场：从 5 台到 50 台的自动化调度</title>
    <link href="https://Scofieldsu.github.io/2025/02/20/%E6%89%8B%E6%9C%BA%E8%AE%BE%E5%A4%87%E5%86%9C%E5%9C%BA%EF%BC%9A%E4%BB%8E5%E5%8F%B0%E5%88%B050%E5%8F%B0%E7%9A%84%E8%87%AA%E5%8A%A8%E5%8C%96%E8%B0%83%E5%BA%A6/"/>
    <id>https://Scofieldsu.github.io/2025/02/20/手机设备农场：从5台到50台的自动化调度/</id>
    <published>2025-02-20T20:00:00.000Z</published>
    <updated>2026-09-06T03:44:51.439Z</updated>
    
    <content type="html"><![CDATA[<p>App 测试绕不开&quot;设备矩阵&quot;：Android 高低端 × 不同 ROM × iOS，单台机器上的设备池撑不住回归规模。2025 年把设备池从 5 台扩到 50 台，调度层完全重做，这篇讲设备农场的架构和几个绕不开的坑。</p><h2 id="一-设备池的抽象设备-资源不是手机"><a class="markdownIt-Anchor" href="#一-设备池的抽象设备-资源不是手机"></a> 一、设备池的抽象：设备 = 资源，不是&quot;手机&quot;</h2><p>第一原则：<strong>调度中心眼里只有&quot;设备句柄&quot;，不关心它是 ADB 序列号还是 USB 口还是云真机</strong>。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@dataclass</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Device</span>:</span></span><br><span class="line">    handle: <span class="built_in">str</span>          <span class="comment"># adb:R5CT123AB / usb:001 / cloud:vm-7</span></span><br><span class="line">    platform: <span class="built_in">str</span>        <span class="comment"># android / ios</span></span><br><span class="line">    spec: <span class="built_in">dict</span>           <span class="comment"># &#123;rom, resolution, cpu_tier: low/mid/high, ram&#125;</span></span><br><span class="line">    location: <span class="built_in">str</span>        <span class="comment"># rack-a / rack-b / cloud</span></span><br><span class="line">    state: DeviceState   <span class="comment"># idle / busy / install / fault / maintenance</span></span><br><span class="line">    capabilities: <span class="built_in">list</span>   <span class="comment"># [gps_mock, multi_open, root]</span></span><br></pre></td></tr></table></figure><p><strong>capability 标签</strong>是调度的关键维度：用例声明自己需要哪些能力（多开用例要 <code>multi_open</code>，LBS 用例要 <code>gps_mock</code>），调度器做能力匹配。设备&quot;是什么&quot;（序列号）只出现在最底层的连接层，上层永远只见标签——这样加一批新设备，只要标签体系不变，调度零改动。</p><h2 id="二-调度三维匹配-亲和性"><a class="markdownIt-Anchor" href="#二-调度三维匹配-亲和性"></a> 二、调度：三维匹配 + 亲和性</h2><p>任务到设备的匹配规则，按优先级：</p><ol><li><strong>硬约束</strong>：平台 + capability 不匹配直接排除；</li><li><strong>亲和性</strong>：同批次任务尽量落到同一设备/同一机架——减少&quot;设备预热&quot;和跨机架的 ADB 转发开销；</li><li><strong>负载均衡</strong>：候选设备里选<strong>最近 1 小时利用率最低</strong>的；</li><li><strong>故障规避</strong>：近 24h 有 fault 记录的设备降权（不是拉黑，fault 原因可能是偶发）。</li></ol><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">pick_device</span>(<span class="params">task, pool</span>):</span></span><br><span class="line">    candidates = [d <span class="keyword">for</span> d <span class="keyword">in</span> pool</span><br><span class="line">                  <span class="keyword">if</span> d.state <span class="keyword">is</span> idle</span><br><span class="line">                  <span class="keyword">and</span> d.platform == task.platform</span><br><span class="line">                  <span class="keyword">and</span> task.need ⊆ d.capabilities]</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">min</span>(candidates, key=<span class="keyword">lambda</span> d: (</span><br><span class="line">        -affinity(d, task.batch), d.load_1h, fault_score(d)))</span><br></pre></td></tr></table></figure><p><strong>为什么亲和性排第一</strong>：App 首次冷启动比热启动慢 3~5 秒，一批 50 个用例如果散落在 50 台设备上，光冷启动就多出 2~3 分钟，而且每台设备的 ROM 差异会把&quot;环境问题&quot;和&quot;产品问题&quot;混在一起。<strong>同批同设备（或同 ROM 设备）是失败归因的便宜保险</strong>。</p><h2 id="三-设备健康主动巡检不是等用例挂"><a class="markdownIt-Anchor" href="#三-设备健康主动巡检不是等用例挂"></a> 三、设备健康：主动巡检，不是等用例挂</h2><p>设备故障是无声的——屏幕黑了、触摸漂移了、ADB 断连了，都表现为&quot;用例随机失败&quot;。被动等失败归因太贵，建主动巡检：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">HEALTH_CHECKS = [</span><br><span class="line">    (<span class="string">&quot;adb_state&quot;</span>,    check_adb_online,        <span class="number">30</span>),   <span class="comment"># 每 30s</span></span><br><span class="line">    (<span class="string">&quot;screen_on&quot;</span>,    check_screen_brightness, <span class="number">60</span>),   <span class="comment"># 每 60s</span></span><br><span class="line">    (<span class="string">&quot;touch&quot;</span>,        check_tap_echo,          <span class="number">300</span>),  <span class="comment"># 每 5min：点一下看 UI 响应</span></span><br><span class="line">    (<span class="string">&quot;storage&quot;</span>,      check_storage_free,      <span class="number">3600</span>), <span class="comment"># 每 1h：磁盘 &lt;2G 告警</span></span><br><span class="line">    (<span class="string">&quot;battery&quot;</span>,      check_battery_plugged,   <span class="number">300</span>),  <span class="comment"># 充电断开立即标记</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure><p>巡检结果进设备状态机：单项失败 → <code>degraded</code>（仍可接单但降权）；连续 3 项或核心项（adb/touch）失败 → <code>fault</code> 自动摘除 + 工单。<strong>关键经验</strong>：<code>degraded</code> 状态比 fault 更有价值——80% 的设备问题（触摸漂移、响应变慢）在 degraded 期就能拦住，等 fault 时设备早就&quot;看起来正常&quot;地污染了一批用例。</p><h2 id="四-50-台之后才暴露的问题"><a class="markdownIt-Anchor" href="#四-50-台之后才暴露的问题"></a> 四、50 台之后才暴露的问题</h2><table><thead><tr><th>问题</th><th>症状</th><th>解法</th></tr></thead><tbody><tr><td>ADB 风暴</td><td>50 台并发 dump 窗口，USB 3.0 hub 全卡死</td><td>dump 请求按机架分桶限流（每桶并发 ≤8），加 100ms 抖动</td></tr><tr><td>屏幕镜像带宽</td><td>实时投屏 50 路，管理网 1Gbps 打满</td><td>投屏默认关，按需开；开时限制 15fps/720p</td></tr><tr><td>热堆积</td><td>机房设备连续跑 4h，CPU 降频，用例耗时 +40%</td><td>批次间强制冷却 10min；机房风扇按温度联动</td></tr><tr><td>数据残留</td><td>上一任务登录态没清，下一任务&quot;已登录&quot;直接跑</td><td>任务结束钩子：强制卸载重装或清数据，<strong>由调度器执行不信任用例</strong></td></tr></tbody></table><h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2><p>设备农场的三个层次：<strong>抽象</strong>（设备 = 带标签的句柄，新设备零代码接入）、<strong>调度</strong>（能力硬约束 + 亲和性 + 负载）、<strong>健康</strong>（主动巡检 + degraded 降级）。规模从 5 到 50 台，代码量翻了 3 倍，但用例层零改动——抽象做对了，扩容就是加标签的事。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;App 测试绕不开&amp;quot;设备矩阵&amp;quot;：Android 高低端 × 不同 ROM × iOS，单台机器上的设备池撑不住回归规模。2025 年把设备池从 5 台扩到 50 台，调度层完全重做，这篇讲设备农场的架构和几个绕不开的坑。&lt;/p&gt;
&lt;h2 id=&quot;一-设备
      
    
    </summary>
    
      <category term="设备管理" scheme="https://Scofieldsu.github.io/categories/%E8%AE%BE%E5%A4%87%E7%AE%A1%E7%90%86/"/>
    
    
      <category term="自动化" scheme="https://Scofieldsu.github.io/tags/%E8%87%AA%E5%8A%A8%E5%8C%96/"/>
    
      <category term="设备农场" scheme="https://Scofieldsu.github.io/tags/%E8%AE%BE%E5%A4%87%E5%86%9C%E5%9C%BA/"/>
    
      <category term="调度" scheme="https://Scofieldsu.github.io/tags/%E8%B0%83%E5%BA%A6/"/>
    
  </entry>
  
  <entry>
    <title>测试数据工厂：解决自动化里最脏的问题</title>
    <link href="https://Scofieldsu.github.io/2024/12/05/%E6%B5%8B%E8%AF%95%E6%95%B0%E6%8D%AE%E5%B7%A5%E5%8E%82%EF%BC%9A%E8%A7%A3%E5%86%B3%E8%87%AA%E5%8A%A8%E5%8C%96%E9%87%8C%E6%9C%80%E8%84%8F%E7%9A%84%E9%97%AE%E9%A2%98/"/>
    <id>https://Scofieldsu.github.io/2024/12/05/测试数据工厂：解决自动化里最脏的问题/</id>
    <published>2024-12-05T19:30:00.000Z</published>
    <updated>2026-09-06T03:44:51.439Z</updated>
    
    <content type="html"><![CDATA[<p>做自动化两年，越来越确信：<strong>数据问题是自动化稳定性的第一来源，比 UI 变化更频繁、比网络问题更隐蔽</strong>。用例挂掉，排查路径经常是&quot;选择器没问题 → 时序没问题 → 数据不对&quot;。这篇讲我们建的&quot;测试数据工厂&quot;，把数据从用例里彻底剥离。</p><h2 id="一-数据问题的三个典型形态"><a class="markdownIt-Anchor" href="#一-数据问题的三个典型形态"></a> 一、数据问题的三个典型形态</h2><ol><li><strong>用例间污染</strong>：A 用例创建了&quot;测试店铺&quot;，B 用例也创建，C 用例的全局列表里出现两个，断言数量直接挂；</li><li><strong>前置数据缺失</strong>：用例假设&quot;有一个带 3 条消息的会话&quot;，但环境里这个会话上周被人手动删了——用例挂了，日志却指向 UI；</li><li><strong>数据规模失真</strong>：列表页用例永远只跑 3 条数据，线上 3000 条时的分页、懒加载、性能问题全测不到。</li></ol><p>三个形态的共同点：<strong>用例自己造数据、清数据</strong>，造的方式散落各处，没人知道环境里此刻有哪些数据。</p><h2 id="二-数据工厂的核心模型profile-factory-cleanup"><a class="markdownIt-Anchor" href="#二-数据工厂的核心模型profile-factory-cleanup"></a> 二、数据工厂的核心模型：Profile + Factory + Cleanup</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 数据声明（YAML，和用例分离）</span></span><br><span class="line"><span class="comment"># data/profiles/conversation_with_msgs.yaml</span></span><br><span class="line">name: conversation_with_msgs</span><br><span class="line">desc: 一个带 N 条消息的活跃会话</span><br><span class="line">params:</span><br><span class="line">  msg_count: &#123; default: <span class="number">3</span>, <span class="built_in">type</span>: <span class="built_in">int</span> &#125;</span><br><span class="line">  with_media: &#123; default: false, <span class="built_in">type</span>: <span class="built_in">bool</span> &#125;</span><br><span class="line"></span><br><span class="line">build:                       <span class="comment"># 构造步骤（工厂执行）</span></span><br><span class="line">  - create_conversation</span><br><span class="line">  - send_messages &#123; count: <span class="string">&quot;$&#123;msg_count&#125;&quot;</span>, media: <span class="string">&quot;$&#123;with_media&#125;&quot;</span> &#125;</span><br><span class="line"></span><br><span class="line">teardown:                    <span class="comment"># 清理步骤（对称）</span></span><br><span class="line">  - delete_conversation</span><br></pre></td></tr></table></figure><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 用例里只声明依赖，不写构造代码</span></span><br><span class="line"><span class="meta">@pytest.mark.data(<span class="params"><span class="string">&quot;conversation_with_msgs&quot;</span></span>)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test_message_list_render</span>(<span class="params">app</span>):</span></span><br><span class="line">    conv = data.current(<span class="string">&quot;conversation_with_msgs&quot;</span>)   <span class="comment"># 工厂已建好，直接引用</span></span><br><span class="line">    open_conversation(app, conv.<span class="built_in">id</span>)</span><br><span class="line">    <span class="keyword">assert</span> message_count(app) == conv.msg_count</span><br></pre></td></tr></table></figure><p>设计要点：</p><ul><li><strong>Profile 是数据契约</strong>：用例声明&quot;我要什么数据&quot;（名字 + 参数），不关心怎么造。数据构造逻辑变更（接口改版）只改 Profile，用例零改动；</li><li><strong>build/teardown 对称</strong>：每个构造步骤必须有对应清理，工厂强制校验——不对称的 Profile 不许入库；</li><li><strong>数据对象带元数据</strong>：<code>conv.id / conv.msg_count / conv.created_at</code> 全部结构化，用例断言引用的是数据对象的真实值，<strong>永远不写死</strong> &quot;3 条消息&quot;这种字面量。</li></ul><h2 id="三-隔离命名空间-租约"><a class="markdownIt-Anchor" href="#三-隔离命名空间-租约"></a> 三、隔离：命名空间 + 租约</h2><p>多组用例并发跑（分布式执行机场景）时，数据必须互相看不见：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DataFactory</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">provision</span>(<span class="params">self, profile, namespace</span>):</span></span><br><span class="line">        <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">        namespace = &lt;run_id&gt;/&lt;worker_id&gt;，工厂给该命名空间内</span></span><br><span class="line"><span class="string">        所有实体注入前缀：shop_8f3a21_b0、conv_8f3a21_b0_1 ...</span></span><br><span class="line"><span class="string">        &quot;&quot;&quot;</span></span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">lease</span>(<span class="params">self, namespace</span>):</span></span><br><span class="line">        <span class="string">&quot;&quot;&quot;租约：30 分钟无心跳自动回收，防 worker 崩溃后数据泄漏&quot;&quot;&quot;</span></span><br><span class="line">        ...</span><br></pre></td></tr></table></figure><ul><li><strong>命名空间前缀</strong>是隔离的基本盘：所有实体名带 <code>run_id</code>，用例之间、批次之间物理隔离，全局列表页也不会互相污染；</li><li><strong>租约回收</strong>兜底清理失败：teardown 没跑完（崩溃/取消）的命名空间，后台任务按租约过期强制回收——<strong>数据泄漏从&quot;积累到出问题&quot;变成&quot;最多存活 30 分钟&quot;</strong>；</li><li>隔离的代价是数据变多，所以工厂同时提供<strong>共享池</strong>：只读型大数据集（3000 条列表、10 万条用户）按版本预生成、全命名空间共享，只有&quot;会被用例修改&quot;的数据才走隔离。</li></ul><h2 id="四-数据规模profile-参数化出压力档"><a class="markdownIt-Anchor" href="#四-数据规模profile-参数化出压力档"></a> 四、数据规模：Profile 参数化出&quot;压力档&quot;</h2><p>&quot;数据规模失真&quot;的解法是把规模变成 Profile 参数，CI 分档跑：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># data/profiles/order_list.yaml</span></span><br><span class="line"><span class="attr">params:</span></span><br><span class="line">  <span class="attr">order_count:</span> &#123; <span class="attr">default:</span> <span class="number">20</span> &#125;   <span class="comment"># 日常档：20 单，用例秒级跑完</span></span><br></pre></td></tr></table></figure><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># CI 配置</span></span><br><span class="line">smoke:  <span class="attr">data_overrides</span> = order_list.order_count=<span class="number">5</span></span><br><span class="line">api:    <span class="attr">data_overrides</span> = order_list.order_count=<span class="number">50</span></span><br><span class="line">regression: <span class="attr">data_overrides</span> = order_list.order_count=<span class="number">3000</span>   <span class="comment"># 发版前才跑大规模</span></span><br></pre></td></tr></table></figure><p><strong>大规模数据只在发版前跑</strong>是成本与收益的平衡：日常 CI 用 20 条验证逻辑，发版回归用 3000 条验证分页/懒加载/性能。数据规模第一次成了 CI 配置里的显式参数，而不是&quot;环境里恰好有多少&quot;。</p><h2 id="五-落地效果"><a class="markdownIt-Anchor" href="#五-落地效果"></a> 五、落地效果</h2><table><thead><tr><th>指标</th><th>数据工厂前</th><th>后</th></tr></thead><tbody><tr><td>用例失败中&quot;数据原因&quot;占比</td><td>~40%</td><td>&lt;8%</td></tr><tr><td>环境数据泄漏清理</td><td>人工，每周 1 次</td><td>租约自动，零人工</td></tr><tr><td>新增用例的数据准备时间</td><td>0.5~2 小时（手工造数据）</td><td>声明 Profile，分钟级</td></tr><tr><td>跨用例污染类 bug</td><td>偶发难查</td><td>命名空间隔离后归零</td></tr></tbody></table><h2 id="六-小结"><a class="markdownIt-Anchor" href="#六-小结"></a> 六、小结</h2><p>数据工厂的本质是<strong>把&quot;数据&quot;从用例的执行逻辑里提升为一等公民</strong>：有契约（Profile）、有生命周期（build/teardown/租约）、有隔离（命名空间）、有规模参数。用例从此只表达&quot;业务断言&quot;，数据问题不再污染测试结论——这是所有测试平台里投入产出比最高的基础设施。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;做自动化两年，越来越确信：&lt;strong&gt;数据问题是自动化稳定性的第一来源，比 UI 变化更频繁、比网络问题更隐蔽&lt;/strong&gt;。用例挂掉，排查路径经常是&amp;quot;选择器没问题 → 时序没问题 → 数据不对&amp;quot;。这篇讲我们建的&amp;quot;测试数据工厂&amp;quot
      
    
    </summary>
    
      <category term="测试工程" scheme="https://Scofieldsu.github.io/categories/%E6%B5%8B%E8%AF%95%E5%B7%A5%E7%A8%8B/"/>
    
    
      <category term="工程化" scheme="https://Scofieldsu.github.io/tags/%E5%B7%A5%E7%A8%8B%E5%8C%96/"/>
    
      <category term="测试数据" scheme="https://Scofieldsu.github.io/tags/%E6%B5%8B%E8%AF%95%E6%95%B0%E6%8D%AE/"/>
    
      <category term="数据工厂" scheme="https://Scofieldsu.github.io/tags/%E6%95%B0%E6%8D%AE%E5%B7%A5%E5%8E%82/"/>
    
  </entry>
  
  <entry>
    <title>桌面 RPA 测试工程化：把脚本变成可度量的测试资产</title>
    <link href="https://Scofieldsu.github.io/2024/09/12/%E6%A1%8C%E9%9D%A2RPA%E6%B5%8B%E8%AF%95%E5%B7%A5%E7%A8%8B%E5%8C%96%EF%BC%9A%E6%8A%8A%E8%84%9A%E6%9C%AC%E5%8F%98%E6%88%90%E5%8F%AF%E5%BA%A6%E9%87%8F%E7%9A%84%E6%B5%8B%E8%AF%95%E8%B5%84%E4%BA%A7/"/>
    <id>https://Scofieldsu.github.io/2024/09/12/桌面RPA测试工程化：把脚本变成可度量的测试资产/</id>
    <published>2024-09-12T20:00:00.000Z</published>
    <updated>2026-09-06T03:44:51.439Z</updated>
    
    <content type="html"><![CDATA[<p>RPA 脚本最常见的死法不是写不出来，而是<strong>写出来之后没人敢动它</strong>：没有版本管理、没有测试、没有监控，跑挂了靠人肉发现。RPA 和自动化测试是同一件事的两面，2024 年把 RPA 脚本按测试工程的标准重做了一遍，这篇讲&quot;工程化&quot;到底改了哪些地方。</p><h2 id="一-脚本分层入口-流程-原子操作"><a class="markdownIt-Anchor" href="#一-脚本分层入口-流程-原子操作"></a> 一、脚本分层：入口 / 流程 / 原子操作</h2><p>RPA 脚本膨胀的根源是&quot;一个脚本干所有事&quot;。分层之后：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">rpa/</span><br><span class="line">├── entry/                    # 入口层：参数解析、启动、退出、上报</span><br><span class="line">│   └── main.py</span><br><span class="line">├── flows/                    # 流程层：业务步骤编排（不含控件细节）</span><br><span class="line">│   └── order_reconciliation.py</span><br><span class="line">├── atoms/                    # 原子层：单控件/单窗口操作（纯函数式）</span><br><span class="line">│   ├── window.py             #   找到窗口/等待/前置</span><br><span class="line">│   ├── grid.py               #   表格取值/翻页/筛选</span><br><span class="line">│   └── dialog.py             #   弹窗确认/输入</span><br><span class="line">└── knowledge/                # 知识库：窗口特征、控件定位、业务常量</span><br><span class="line">    └── windows.yaml</span><br></pre></td></tr></table></figure><p>分层规则：</p><ul><li><strong>入口层</strong>只做三件事：吃参数、调一个 flow、把结果和状态码上报——入口挂了重启无副作用；</li><li><strong>流程层</strong>编排业务语义（“核对订单”= 打开系统 → 拉取清单 → 逐单核对 → 输出差异），<strong>不出现任何控件名</strong>；</li><li><strong>原子层</strong>是唯一的控件接触面，每个原子操作幂等、可单独重放；</li><li><strong>知识库</strong>把&quot;窗口长什么样、控件在哪&quot;从代码里抽出来，UI 微调改 YAML 不改代码。</li></ul><h2 id="二-状态机rpa-脚本必须是可恢复的"><a class="markdownIt-Anchor" href="#二-状态机rpa-脚本必须是可恢复的"></a> 二、状态机：RPA 脚本必须是可恢复的</h2><p>UI 自动化脚本崩一次就从头来，RPA 不行——一个跑 3 小时的核对任务，第 2 小时崩了从头跑等于没做。核心是<strong>流程级检查点</strong>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">FlowRunner</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, flow_id</span>):</span></span><br><span class="line">        self.ckpt = CheckpointStore(<span class="string">f&quot;var/run/<span class="subst">&#123;flow_id&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run_step</span>(<span class="params">self, step_id, fn</span>):</span></span><br><span class="line">        <span class="keyword">if</span> self.ckpt.done(step_id):</span><br><span class="line">            <span class="keyword">return</span> self.ckpt.result(step_id)    <span class="comment"># 已完成：直接取结果</span></span><br><span class="line">        result = fn()                            <span class="comment"># 执行</span></span><br><span class="line">        self.ckpt.mark(step_id, result)          <span class="comment"># 落盘（原子写：tmp + rename）</span></span><br><span class="line">        <span class="keyword">return</span> result</span><br><span class="line"></span><br><span class="line"><span class="comment"># flows/order_reconciliation.py</span></span><br><span class="line">runner.run_step(<span class="string">&quot;fetch_list&quot;</span>, fetch_order_list)</span><br><span class="line"><span class="keyword">for</span> batch <span class="keyword">in</span> batches:</span><br><span class="line">    runner.run_step(<span class="string">f&quot;reconcile:<span class="subst">&#123;batch.<span class="built_in">id</span>&#125;</span>&quot;</span>, <span class="keyword">lambda</span> b=batch: reconcile(b))</span><br><span class="line">runner.run_step(<span class="string">&quot;report&quot;</span>, build_report)</span><br></pre></td></tr></table></figure><p>检查点粒度按&quot;可重放且幂等&quot;切：拉取清单（幂等）是检查点，逐批核对（幂等）每批一个检查点，报告生成（幂等）一个检查点。<strong>不可幂等的操作（如&quot;发送通知&quot;）必须设计成可重发</strong>（带幂等键），否则那个检查点不能落。</p><p>崩溃恢复 = 重启入口 → 检查点续跑。实测把 3 小时任务的&quot;崩溃代价&quot;从 3 小时降到平均 11 分钟（一个检查点区间）。</p><h2 id="三-失败分类不是所有失败都要告警"><a class="markdownIt-Anchor" href="#三-失败分类不是所有失败都要告警"></a> 三、失败分类：不是所有失败都要告警</h2><p>RPA 跑在生产机器上，告警风暴比失败本身更伤。失败四分类，处置各不同：</p><table><thead><tr><th>类别</th><th>判定</th><th>处置</th></tr></thead><tbody><tr><td><strong>瞬时</strong></td><td>窗口加载慢、网络抖动、目标程序未启动</td><td>自动重试（指数退避 3 次），成功则静默记录</td></tr><tr><td><strong>环境</strong></td><td>目标程序版本变了、控件找不到、分辨率异常</td><td>截图 + 控件树 dump，<strong>暂停任务</strong>，钉钉通知值班</td></tr><tr><td><strong>业务</strong></td><td>数据校验不通过（订单对不上）</td><td>这是 RPA 的<strong>产出</strong>不是错误，进差异报告</td></tr><tr><td><strong>致命</strong></td><td>目标系统整体不可用、证书过期</td><td>任务终止，升级告警</td></tr></tbody></table><p>分类靠<strong>错误码约定</strong>：原子层抛异常时带类别（<code>RpaError(category, code, context)</code>），入口层按类别走处置表。&quot;业务失败 ≠ 脚本失败&quot;是 RPA 和测试最容易被混淆的点——测试里 assert 失败是期望中的价值，RPA 的&quot;核对出差异&quot;同理，不能当故障报。</p><h2 id="四-可度量rpa-的-slo"><a class="markdownIt-Anchor" href="#四-可度量rpa-的-slo"></a> 四、可度量：RPA 的 SLO</h2><p>工程化的最后一步是给 RPA 定 SLO，和线上服务一样看板化：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># RPA SLO（月度）</span></span><br><span class="line"><span class="attr">task_success_rate:</span> <span class="number">0.995</span>        <span class="comment"># 任务级成功率（排除业务差异）</span></span><br><span class="line"><span class="attr">first_pass_rate:</span> <span class="number">0.97</span>           <span class="comment"># 一次通过率（不含重试成功）</span></span><br><span class="line"><span class="attr">recovery_rate:</span> <span class="number">0.95</span>             <span class="comment"># 检查点恢复成功率</span></span><br><span class="line"><span class="attr">mttr_minutes:</span> <span class="number">15</span>                <span class="comment"># 失败到人工介入的时长</span></span><br></pre></td></tr></table></figure><p>看板指标：</p><ul><li><strong>任务成功率</strong> = 成功任务 / 计划任务（业务差异不算失败）；</li><li><strong>一次通过率</strong>反映脚本真实健壮性——重试成功率高 = 脚本在吃重试，该修选择器/时序了；</li><li><strong>原子操作耗时分布</strong>：P95 耗时翻倍往往是&quot;UI 变重了&quot;的早期信号，比等失败更早发现问题。</li></ul><h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2><p>RPA 工程化四件事：<strong>分层</strong>（入口/流程/原子/知识库，控件只出现在原子层）、<strong>检查点</strong>（可恢复，崩溃代价从小时级降到分钟级）、<strong>失败分类</strong>（业务差异不是故障）、<strong>SLO 看板</strong>（一次通过率、恢复率）。做完这四件，RPA 脚本才配叫&quot;资产&quot;——可以交接、可以演进、可以信任。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;RPA 脚本最常见的死法不是写不出来，而是&lt;strong&gt;写出来之后没人敢动它&lt;/strong&gt;：没有版本管理、没有测试、没有监控，跑挂了靠人肉发现。RPA 和自动化测试是同一件事的两面，2024 年把 RPA 脚本按测试工程的标准重做了一遍，这篇讲&amp;quot;工程化&amp;qu
      
    
    </summary>
    
      <category term="RPA" scheme="https://Scofieldsu.github.io/categories/RPA/"/>
    
    
      <category term="桌面自动化" scheme="https://Scofieldsu.github.io/tags/%E6%A1%8C%E9%9D%A2%E8%87%AA%E5%8A%A8%E5%8C%96/"/>
    
      <category term="RPA" scheme="https://Scofieldsu.github.io/tags/RPA/"/>
    
      <category term="工程化" scheme="https://Scofieldsu.github.io/tags/%E5%B7%A5%E7%A8%8B%E5%8C%96/"/>
    
  </entry>
  
  <entry>
    <title>Locust 性能测试实战：从脚本到可复现的压测报告</title>
    <link href="https://Scofieldsu.github.io/2024/06/15/Locust%E6%80%A7%E8%83%BD%E6%B5%8B%E8%AF%95%E5%AE%9E%E6%88%98%EF%BC%9A%E4%BB%8E%E8%84%9A%E6%9C%AC%E5%88%B0%E5%8F%AF%E5%A4%8D%E7%8E%B0%E7%9A%84%E5%8E%8B%E6%B5%8B%E6%8A%A5%E5%91%8A/"/>
    <id>https://Scofieldsu.github.io/2024/06/15/Locust性能测试实战：从脚本到可复现的压测报告/</id>
    <published>2024-06-15T20:40:00.000Z</published>
    <updated>2026-09-06T03:44:51.437Z</updated>
    
    <content type="html"><![CDATA[<p>压测最常见的翻车不是脚本写不出来，而是&quot;结论没法复现&quot;——同一脚本今天 P99 800ms 明天 P99 2s，谁也说不清是系统变了还是测试本身的问题。这篇讲用 Locust 做压测时，如何把&quot;脚本&quot;升级成&quot;可复现的实验&quot;。</p><h2 id="一-脚本设计用户模型不是请求列表"><a class="markdownIt-Anchor" href="#一-脚本设计用户模型不是请求列表"></a> 一、脚本设计：用户模型，不是请求列表</h2><p>Locust 的 <code>HttpUser</code> 是&quot;一个真实用户的行为模型&quot;，不是接口调用清单：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ChatUser</span>(<span class="params">HttpUser</span>):</span></span><br><span class="line">    wait_time = between(<span class="number">2</span>, <span class="number">8</span>)          <span class="comment"># 操作间隔：真实用户不是机器</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">on_start</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="comment"># 登录 + 拉初始会话列表：每个虚拟用户的&quot;开场动作&quot;</span></span><br><span class="line">        self.client.post(<span class="string">&quot;/api/login&quot;</span>, json=self.cred)</span><br><span class="line">        self.client.get(<span class="string">&quot;/api/conversations&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">    @task(<span class="params"><span class="number">3</span></span>)   </span><span class="comment"># 权重 3</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">open_conversation</span>(<span class="params">self</span>):</span></span><br><span class="line">        cid = self.random_conversation()</span><br><span class="line">        self.client.get(<span class="string">f&quot;/api/conversations/<span class="subst">&#123;cid&#125;</span>/messages?limit=20&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">    @task(<span class="params"><span class="number">1</span></span>)   </span><span class="comment"># 权重 1</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">send_message</span>(<span class="params">self</span>):</span></span><br><span class="line">        cid = self.random_conversation()</span><br><span class="line">        self.client.post(<span class="string">f&quot;/api/conversations/<span class="subst">&#123;cid&#125;</span>/messages&quot;</span>, json=&#123;...&#125;)</span><br><span class="line"></span><br><span class="line"><span class="meta">    @task(<span class="params"><span class="number">1</span></span>)</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">search</span>(<span class="params">self</span>):</span></span><br><span class="line">        self.client.get(<span class="string">f&quot;/api/search?q=<span class="subst">&#123;self.random_keyword()&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure><p><strong>要点</strong>：</p><ul><li><strong>权重比就是流量模型</strong>：开会话 : 发消息 : 搜索 = 3:1:1，这个数字应该来自线上 access log 的统计，不是拍脑袋；</li><li><code>wait_time</code> 用分布而不是固定值：<code>between(2,8)</code> 模拟人类节奏，固定 3s 会让 QPS 曲线出现假性规律；</li><li><code>on_start</code> 里的登录不要放进 <code>@task</code>——登录只该发生一次，否则登录接口 QPS 被虚高 10 倍。</li></ul><h2 id="二-压测曲线阶梯式不要一上来满负载"><a class="markdownIt-Anchor" href="#二-压测曲线阶梯式不要一上来满负载"></a> 二、压测曲线：阶梯式，不要一上来满负载</h2><p>&quot;直接上 500 并发看崩溃&quot;得不到任何有用结论。阶梯曲线每一级回答一个问题：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># locustfile 外部控制（locust -f locustfile.py --headless 不适用曲线控制，</span></span><br><span class="line"><span class="comment"># 用 locust 的 custom shapes 或外部脚本调 REST API 改并发数）</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">StepLoadShape</span>(<span class="params">LoadTestShape</span>):</span></span><br><span class="line">    STEPS = [(<span class="number">50</span>, <span class="number">300</span>), (<span class="number">100</span>, <span class="number">300</span>), (<span class="number">200</span>, <span class="number">300</span>), (<span class="number">400</span>, <span class="number">300</span>)]   <span class="comment"># (并发, 持续秒)</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">tick</span>(<span class="params">self</span>):</span></span><br><span class="line">        elapsed = time.time() - self.start_time</span><br><span class="line">        <span class="keyword">for</span> users, duration <span class="keyword">in</span> self.STEPS:</span><br><span class="line">            <span class="keyword">if</span> elapsed &lt; duration:</span><br><span class="line">                <span class="keyword">return</span> users</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span>    <span class="comment"># 全部阶梯完成，结束</span></span><br></pre></td></tr></table></figure><p>每级阶梯记录：<strong>平均 RT / P95 / P99 / 错误率 / 依赖侧 CPU</strong>。判读规则：</p><ul><li>P99 开始非线性上涨的那一级 = <strong>拐点</strong>，拐点并发数就是容量的工作值；</li><li>错误率在拐点前就出现（如 504）= 依赖先于自身瓶颈（DB 连接池、下游限流），去查依赖；</li><li>每级之间留 30s 的&quot;稳定窗口&quot;再采样，爬坡期的数据不要进结论。</li></ul><h2 id="三-复现性压测报告必须附实验元数据"><a class="markdownIt-Anchor" href="#三-复现性压测报告必须附实验元数据"></a> 三、复现性：压测报告必须附&quot;实验元数据&quot;</h2><p>这是&quot;结论没法复现&quot;的直接解药——每次压测落一份元数据：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">run_id:</span> <span class="string">perf-20240615-02</span></span><br><span class="line"><span class="attr">timestamp:</span> <span class="number">2024-06-15T20:40:00+08:00</span></span><br><span class="line"><span class="attr">target:</span></span><br><span class="line">  <span class="attr">base_url:</span> <span class="string">https://staging.api.xxx.com</span></span><br><span class="line">  <span class="attr">git_sha:</span> <span class="string">a3f9c21</span>            <span class="comment"># 被测系统版本（问运维要）</span></span><br><span class="line">  <span class="attr">deploy_env:</span> <span class="string">staging</span></span><br><span class="line"><span class="attr">locust:</span></span><br><span class="line">  <span class="attr">version:</span> <span class="number">2.24</span><span class="number">.1</span></span><br><span class="line">  <span class="attr">users_curve:</span> [<span class="number">50</span>, <span class="number">100</span>, <span class="number">200</span>, <span class="number">400</span>]</span><br><span class="line">  <span class="attr">spawn_rate:</span> <span class="number">10</span></span><br><span class="line">  <span class="attr">host_overrides:</span> []</span><br><span class="line"><span class="attr">data:</span></span><br><span class="line">  <span class="attr">user_pool:</span> <span class="string">&quot;perf_users_500&quot;</span>  <span class="comment"># 测试账号池（预生成，登录态预置）</span></span><br><span class="line">  <span class="attr">conversation_pool:</span> <span class="number">200</span>       <span class="comment"># 预热的会话数据量</span></span><br><span class="line"><span class="attr">metrics:</span></span><br><span class="line">  <span class="attr">at_200_users:</span> &#123;<span class="attr">p95:</span> <span class="number">180</span>, <span class="attr">p99:</span> <span class="number">410</span>, <span class="attr">err_rate:</span> <span class="number">0.0</span>&#125;</span><br><span class="line">  <span class="attr">at_400_users:</span> &#123;<span class="attr">p95:</span> <span class="number">620</span>, <span class="attr">p99:</span> <span class="number">1900</span>, <span class="attr">err_rate:</span> <span class="number">0.02</span>&#125;</span><br><span class="line">  <span class="attr">inflection:</span> <span class="number">200</span><span class="number">-400</span>          <span class="comment"># 拐点区间</span></span><br></pre></td></tr></table></figure><p>三条纪律：</p><ol><li><strong>被测系统必须锁定 git sha</strong>——&quot;staging 环境&quot;不是版本，版本是 sha；</li><li><strong>数据池预生成</strong>：测试账号、会话、商品数据在压测前准备好（和 2026 年那篇&quot;数据工厂&quot;衔接），压测中现造数据会把 DB 写入延迟混进结论；</li><li><strong>同机压测要声明</strong>：压测机与被测机同机房/同网段时，结论只对该网络条件成立，元数据里写明拓扑。</li></ol><h2 id="四-常见误读"><a class="markdownIt-Anchor" href="#四-常见误读"></a> 四、常见误读</h2><table><thead><tr><th>现象</th><th>常见误读</th><th>实际可能</th></tr></thead><tbody><tr><td>高并发下 P99 暴涨、P50 正常</td><td>应用慢</td><td>连接池耗尽后的排队（看依赖侧等待时间拆分）</td></tr><tr><td>错误率 0.1% 集中在某接口</td><td>接口偶发 bug</td><td>该接口超时阈值 &lt; 其 P99（阈值和真实分布不匹配）</td></tr><tr><td>阶梯每一级 P95 都涨 20%</td><td>线性劣化</td><td>正常——RT 随并发上升是必然，<strong>斜率突变</strong>才是信号</td></tr><tr><td>压测机 CPU 100%</td><td>系统到瓶颈了</td><td>压测机先饱和，被测系统还没到（查 locust 进程 CPU）</td></tr></tbody></table><h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2><p>Locust 只是&quot;发压的枪&quot;，压测的含金量在<strong>流量模型（权重+节奏）、曲线设计（阶梯+稳定窗）、实验元数据（sha+数据池+拓扑）</strong>。做到这三点，压测报告才是可复现的实验记录，而不是&quot;看起来很快&quot;的截图。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;压测最常见的翻车不是脚本写不出来，而是&amp;quot;结论没法复现&amp;quot;——同一脚本今天 P99 800ms 明天 P99 2s，谁也说不清是系统变了还是测试本身的问题。这篇讲用 Locust 做压测时，如何把&amp;quot;脚本&amp;quot;升级成&amp;quot;可复现的实验&amp;q
      
    
    </summary>
    
      <category term="性能测试" scheme="https://Scofieldsu.github.io/categories/%E6%80%A7%E8%83%BD%E6%B5%8B%E8%AF%95/"/>
    
    
      <category term="Python" scheme="https://Scofieldsu.github.io/tags/Python/"/>
    
      <category term="Locust" scheme="https://Scofieldsu.github.io/tags/Locust/"/>
    
      <category term="性能测试" scheme="https://Scofieldsu.github.io/tags/%E6%80%A7%E8%83%BD%E6%B5%8B%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>跨平台元素定位的统一策略：从 App 到 PC 到 Web</title>
    <link href="https://Scofieldsu.github.io/2024/03/08/%E8%B7%A8%E5%B9%B3%E5%8F%B0%E5%85%83%E7%B4%A0%E5%AE%9A%E4%BD%8D%E7%9A%84%E7%BB%9F%E4%B8%80%E7%AD%96%E7%95%A5%EF%BC%9A%E4%BB%8EApp%E5%88%B0PC%E5%88%B0Web/"/>
    <id>https://Scofieldsu.github.io/2024/03/08/跨平台元素定位的统一策略：从App到PC到Web/</id>
    <published>2024-03-08T19:50:00.000Z</published>
    <updated>2026-09-06T03:44:51.439Z</updated>
    
    <content type="html"><![CDATA[<p>App、PC、Web 三端各写各的定位策略，同一套业务用例的维护成本是三倍的。2024 年把三端的定位层重构成<strong>统一策略 + 自愈选择器</strong>，这篇讲设计思路和落地细节。</p><h2 id="一-统一的定位描述locator-规范"><a class="markdownIt-Anchor" href="#一-统一的定位描述locator-规范"></a> 一、统一的定位描述：Locator 规范</h2><p>三端控件标识符完全不同（resource-id / auto_id / data-testid），但<strong>语义是同一套</strong>。先定义统一的 Locator 描述：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@dataclass</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Locator</span>:</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;业务语义化的定位描述，与具体平台解耦&quot;&quot;&quot;</span></span><br><span class="line">    role: <span class="built_in">str</span>          <span class="comment"># send_button / chat_input / order_grid ...</span></span><br><span class="line">    platform_map: <span class="built_in">dict</span>  <span class="comment"># 平台 -&gt; 具体选择器</span></span><br><span class="line">    fallback: <span class="built_in">list</span>     <span class="comment"># 主选择器失效后的兜底序列</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 用例里只写 role，不写选择器</span></span><br><span class="line">send_btn = page.locate(<span class="string">&quot;send_button&quot;</span>)</span><br></pre></td></tr></table></figure><p><code>platform_map</code> 由<strong>平台适配层</strong>维护，集中一处：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">LOCATOR_REGISTRY = &#123;</span><br><span class="line">    <span class="string">&quot;send_button&quot;</span>: &#123;</span><br><span class="line">        <span class="string">&quot;app&quot;</span>:  &#123;<span class="string">&quot;strategy&quot;</span>: <span class="string">&quot;resource_id&quot;</span>, <span class="string">&quot;value&quot;</span>: <span class="string">&quot;com.app:id/btn_send&quot;</span>&#125;,</span><br><span class="line">        <span class="string">&quot;pc&quot;</span>:   &#123;<span class="string">&quot;strategy&quot;</span>: <span class="string">&quot;auto_id&quot;</span>,     <span class="string">&quot;value&quot;</span>: <span class="string">&quot;btnSend&quot;</span>&#125;,</span><br><span class="line">        <span class="string">&quot;web&quot;</span>:  &#123;<span class="string">&quot;strategy&quot;</span>: <span class="string">&quot;test_id&quot;</span>,     <span class="string">&quot;value&quot;</span>: <span class="string">&quot;send-message-btn&quot;</span>&#125;,</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">&quot;chat_input&quot;</span>: &#123;</span><br><span class="line">        <span class="string">&quot;app&quot;</span>:  &#123;<span class="string">&quot;strategy&quot;</span>: <span class="string">&quot;resource_id&quot;</span>, <span class="string">&quot;value&quot;</span>: <span class="string">&quot;com.app:id/et_msg&quot;</span>&#125;,</span><br><span class="line">        <span class="string">&quot;pc&quot;</span>:   &#123;<span class="string">&quot;strategy&quot;</span>: <span class="string">&quot;auto_id&quot;</span>,     <span class="string">&quot;value&quot;</span>: <span class="string">&quot;edMessage&quot;</span>&#125;,</span><br><span class="line">        <span class="string">&quot;web&quot;</span>:  &#123;<span class="string">&quot;strategy&quot;</span>: <span class="string">&quot;test_id&quot;</span>,     <span class="string">&quot;value&quot;</span>: <span class="string">&quot;message-input&quot;</span>&#125;,</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment"># ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>收益</strong>：改一处选择器，三端用例不动；新人写用例只需要认识 <code>role</code> 词汇表，不用学三个平台的 API。</p><h2 id="二-选择器优先级跨平台统一排序"><a class="markdownIt-Anchor" href="#二-选择器优先级跨平台统一排序"></a> 二、选择器优先级：跨平台统一排序</h2><p>不同平台的&quot;稳定&quot;含义不同，统一排序如下：</p><table><thead><tr><th>优先级</th><th>通用原则</th><th>App 实例</th><th>PC 实例</th><th>Web 实例</th></tr></thead><tbody><tr><td>1</td><td>开发约定的稳定 ID</td><td>resource-id</td><td>auto_id</td><td>data-testid</td></tr><tr><td>2</td><td>无障碍/语义属性</td><td>content-desc</td><td>Name（UIA）</td><td>aria-label</td></tr><tr><td>3</td><td>稳定文案</td><td>text</td><td>title</td><td>text 精确匹配</td></tr><tr><td>4</td><td>结构相对定位</td><td>父级 ID + 索引</td><td>容器 + 相对路径</td><td>CSS 相对选择器</td></tr><tr><td>5</td><td>全文查询兜底</td><td>XPath</td><td>UIA XPath</td><td>XPath/JS 查询</td></tr></tbody></table><p><strong>铁律</strong>：第 1、2 优先级需要开发配合（约定 ID 规范），这是&quot;定位维护成本&quot;从根源上降下来的唯一办法。第 3 优先级以下的选择器全部进&quot;待治理清单&quot;，每个迭代消化一部分。</p><h2 id="三-自愈选择器失效后自动找替代"><a class="markdownIt-Anchor" href="#三-自愈选择器失效后自动找替代"></a> 三、自愈选择器：失效后自动找替代</h2><p>UI 改版后选择器失效是常态，人工修选择器是纯消耗。自愈的思路：<strong>主选择器失效时，用&quot;锚点元素 + 相对关系&quot;重新定位</strong>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">locate_with_healing</span>(<span class="params">role, driver, platform</span>):</span></span><br><span class="line">    primary = LocatorResolver.resolve(role, platform)</span><br><span class="line">    el = try_find(driver, primary, timeout=<span class="number">3</span>)</span><br><span class="line">    <span class="keyword">if</span> el:</span><br><span class="line">        <span class="keyword">return</span> el</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 自愈路径：找锚点，再走相对关系</span></span><br><span class="line">    anchor, relation = LocatorRegistry.healing_info(role)</span><br><span class="line">    anchor_el = try_find(driver, anchor, timeout=<span class="number">3</span>)</span><br><span class="line">    <span class="keyword">if</span> anchor_el <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="keyword">raise</span> LocatorStaleError(role)      <span class="comment"># 锚点也没了 = 大改版，报人工介入</span></span><br><span class="line">    el = relation.apply(anchor_el, driver)  <span class="comment"># 例如：锚点右侧第 2 个可点击控件</span></span><br><span class="line">    <span class="keyword">if</span> el:</span><br><span class="line">        <span class="comment"># 上报自愈事件：记录&quot;新位置&quot;，供下一个人工确认固化</span></span><br><span class="line">        HealingReporter.report(role, platform, primary, el)</span><br><span class="line">        <span class="keyword">return</span> el</span><br><span class="line">    <span class="keyword">raise</span> LocatorStaleError(role)</span><br></pre></td></tr></table></figure><p><strong>关键设计</strong>：自愈不是&quot;随便找一个像的控件点掉&quot;，而是<strong>锚点 + 显式相对关系</strong>（“在 X 右边的第 N 个可点击元素”），关系是人审过的，错了能看出来。每次自愈事件进周报，连续自愈的 role 说明该控件该和开发重新约定 ID 了。</p><p><strong>实测数据</strong>：UI 大改版周，选择器失效 47 处，自愈命中 39 处（83%），人工介入 8 处（其中 5 处是锚点自身失效的大改版）。自愈把&quot;改版周的 2 天修选择器&quot;压到 2 小时。</p><h2 id="四-选择器质量门禁"><a class="markdownIt-Anchor" href="#四-选择器质量门禁"></a> 四、选择器质量门禁</h2><p>定位层重构后，选择器本身也要进质量管控：</p><ol><li><strong>静态检查</strong>（CI lint）：用例里出现裸 XPath/绝对路径直接 fail，必须走 <code>role</code>；</li><li><strong>脆弱度评分</strong>：每个 role 的选择器按优先级打分（1 级=100 分，5 级=40 分），平台选择器均分低于 70 的进治理看板；</li><li><strong>失效归因</strong>：选择器失效分三类统计——“ID 被删”（开发违约，找开发）、“结构变化”（走自愈）、“文案变化”（多语言/运营改文案，换优先级）。归因数据是&quot;ID 规范&quot;能推下去的依据。</li></ol><h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2><p>统一定位层的三个层次：<strong>role 词汇表</strong>（用例与平台解耦）、<strong>优先级排序</strong>（脆弱性可控）、<strong>锚点自愈</strong>（改版周不瘫痪）。成本是一次性的重构（约 3 周），收益是之后每次 UI 改版的人力从&quot;天&quot;变&quot;小时&quot;，而且选择器质量第一次有了可度量的指标。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;App、PC、Web 三端各写各的定位策略，同一套业务用例的维护成本是三倍的。2024 年把三端的定位层重构成&lt;strong&gt;统一策略 + 自愈选择器&lt;/strong&gt;，这篇讲设计思路和落地细节。&lt;/p&gt;
&lt;h2 id=&quot;一-统一的定位描述locator-规范&quot;&gt;&lt;a cl
      
    
    </summary>
    
      <category term="自动化测试" scheme="https://Scofieldsu.github.io/categories/%E8%87%AA%E5%8A%A8%E5%8C%96%E6%B5%8B%E8%AF%95/"/>
    
    
      <category term="元素定位" scheme="https://Scofieldsu.github.io/tags/%E5%85%83%E7%B4%A0%E5%AE%9A%E4%BD%8D/"/>
    
      <category term="自愈选择器" scheme="https://Scofieldsu.github.io/tags/%E8%87%AA%E6%84%88%E9%80%89%E6%8B%A9%E5%99%A8/"/>
    
      <category term="跨平台" scheme="https://Scofieldsu.github.io/tags/%E8%B7%A8%E5%B9%B3%E5%8F%B0/"/>
    
  </entry>
  
  <entry>
    <title>分布式测试执行机：让用例跑在一台机器的 20 倍规模上</title>
    <link href="https://Scofieldsu.github.io/2023/12/20/%E5%88%86%E5%B8%83%E5%BC%8F%E6%B5%8B%E8%AF%95%E6%89%A7%E8%A1%8C%E6%9C%BA%EF%BC%9A%E8%AE%A9%E7%94%A8%E4%BE%8B%E8%B7%91%E5%9C%A8%E4%B8%80%E5%8F%B0%E6%9C%BA%E5%99%A8%E7%9A%8420%E5%80%8D%E8%A7%84%E6%A8%A1%E4%B8%8A/"/>
    <id>https://Scofieldsu.github.io/2023/12/20/分布式测试执行机：让用例跑在一台机器的20倍规模上/</id>
    <published>2023-12-20T20:20:00.000Z</published>
    <updated>2026-09-06T03:44:51.437Z</updated>
    
    <content type="html"><![CDATA[<p>单机跑全量回归要 4 小时，发版窗口只有 90 分钟——分布式执行机不是为了&quot;炫技&quot;，是被发版节奏逼出来的。这篇记录一套轻量分布式测试执行架构：不引入 Kubernetes，N 台 Windows 机器 + 一个调度中心，把 4 小时压进 90 分钟。</p><h2 id="一-架构总览"><a class="markdownIt-Anchor" href="#一-架构总览"></a> 一、架构总览</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">                 ┌─────────────┐</span><br><span class="line">Web 控制台 ─────▶│  调度中心     │◀──── 执行机心跳（10s）</span><br><span class="line">                 │  (Flask)    │────▶ 任务下发（gRPC/HTTP）</span><br><span class="line">                 └──────┬──────┘</span><br><span class="line">          ┌─────────────┼─────────────┐</span><br><span class="line">     ┌────▼────┐   ┌────▼────┐   ┌────▼────┐</span><br><span class="line">     │ 执行机 A │   │ 执行机 B │   │ 执行机 N │  （每台 4~8 个执行槽）</span><br><span class="line">     │ 槽1..4  │   │ 槽1..8  │   │ 槽1..4  │</span><br><span class="line">     └─────────┘   └─────────┘   └─────────┘</span><br></pre></td></tr></table></figure><p>关键决策：<strong>执行机是无状态的 worker</strong>。用例代码、配置、依赖全部从调度中心拉取（git 仓库 + 配置包），执行机只负责&quot;给我一个任务 ID，我跑完把结果吐回来&quot;。加机器 = 装个 agent + 配个 token，零代码变更。</p><h2 id="二-任务切分用例-任务-槽位"><a class="markdownIt-Anchor" href="#二-任务切分用例-任务-槽位"></a> 二、任务切分：用例 → 任务 → 槽位</h2><p>切分粒度决定了并行效率和稳定性，三层结构：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Scheduler</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">split</span>(<span class="params">self, plan: TestPlan</span>) -&gt; <span class="built_in">list</span>[Task]:</span></span><br><span class="line">        <span class="string">&quot;&quot;&quot;切分策略：同平台用例聚组，组内按预估时长贪心装箱&quot;&quot;&quot;</span></span><br><span class="line">        groups = group_by(plan.cases, key=<span class="keyword">lambda</span> c: c.platform)</span><br><span class="line">        tasks = []</span><br><span class="line">        <span class="keyword">for</span> platform, cases <span class="keyword">in</span> groups.items():</span><br><span class="line">            <span class="comment"># 贪心装箱：目标 = 单任务时长 ≤ 15 分钟</span></span><br><span class="line">            <span class="keyword">for</span> box <span class="keyword">in</span> bin_pack(cases, target_minutes=<span class="number">15</span>):</span><br><span class="line">                tasks.append(Task(</span><br><span class="line">                    platform=platform,</span><br><span class="line">                    cases=box,</span><br><span class="line">                    est_minutes=est(box),</span><br><span class="line">                ))</span><br><span class="line">        <span class="keyword">return</span> tasks</span><br></pre></td></tr></table></figure><p><strong>为什么按平台聚组</strong>：同一平台（同一台设备/同一个 App 包）的用例放一个任务，避免一台机器上同时装 4 个不同平台——设备互斥、安装包冲突、代理端口打架，全来自跨平台混跑。</p><p><strong>为什么单任务 15 分钟</strong>：经验值是&quot;失败可定位&quot;的上限——任务跑 15 分钟挂了，回看日志找现场是现实可行的；60 分钟的任务挂了基本只能重跑。</p><h2 id="三-心跳与槽位动态感知道具"><a class="markdownIt-Anchor" href="#三-心跳与槽位动态感知道具"></a> 三、心跳与槽位：动态感知道具</h2><p>执行机 agent 每 10s 上报：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">&quot;agent&quot;</span>: <span class="string">&quot;A-03&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;slots&quot;</span>: [</span><br><span class="line">    &#123;<span class="attr">&quot;id&quot;</span>: <span class="number">1</span>, <span class="attr">&quot;state&quot;</span>: <span class="string">&quot;idle&quot;</span>&#125;,</span><br><span class="line">    &#123;<span class="attr">&quot;id&quot;</span>: <span class="number">2</span>, <span class="attr">&quot;state&quot;</span>: <span class="string">&quot;running&quot;</span>, <span class="attr">&quot;task&quot;</span>: <span class="string">&quot;T-1024&quot;</span>, <span class="attr">&quot;progress&quot;</span>: <span class="number">0.4</span>&#125;,</span><br><span class="line">    &#123;<span class="attr">&quot;id&quot;</span>: <span class="number">3</span>, <span class="attr">&quot;state&quot;</span>: <span class="string">&quot;busy_install&quot;</span>, <span class="attr">&quot;task&quot;</span>: <span class="string">&quot;T-1025&quot;</span>&#125;,</span><br><span class="line">    &#123;<span class="attr">&quot;id&quot;</span>: <span class="number">4</span>, <span class="attr">&quot;state&quot;</span>: <span class="string">&quot;fault&quot;</span>, <span class="attr">&quot;reason&quot;</span>: <span class="string">&quot;device offline&quot;</span>&#125;</span><br><span class="line">  ],</span><br><span class="line">  <span class="attr">&quot;load&quot;</span>: <span class="number">0.6</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>槽位状态机：<code>idle → busy_install → running → done/fault → idle</code>。调度中心只往 <code>idle</code> 槽派任务；<code>fault</code> 槽自动隔离，连续 3 次 fault 的机器<strong>整机摘除</strong>并告警——一台坏机器不应该拖累整批回归。</p><h2 id="四-结果回收与断点"><a class="markdownIt-Anchor" href="#四-结果回收与断点"></a> 四、结果回收与断点</h2><p>每个用例结束立即上报，不等任务结束：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 执行机 agent（节选）</span></span><br><span class="line"><span class="keyword">for</span> case <span class="keyword">in</span> task.cases:</span><br><span class="line">    result = run_case(case)              <span class="comment"># 本地执行，SSE 推实时日志</span></span><br><span class="line">    post(<span class="string">&quot;/api/tasks/result&quot;</span>, &#123;</span><br><span class="line">        <span class="string">&quot;task&quot;</span>: task.<span class="built_in">id</span>, <span class="string">&quot;case&quot;</span>: case.<span class="built_in">id</span>,</span><br><span class="line">        <span class="string">&quot;status&quot;</span>: result.status,         <span class="comment"># passed/failed/skipped</span></span><br><span class="line">        <span class="string">&quot;duration&quot;</span>: result.duration,</span><br><span class="line">        <span class="string">&quot;artifacts&quot;</span>: [screenshot_path, log_path],   <span class="comment"># 上传 OSS 后传 URL</span></span><br><span class="line">    &#125;)</span><br></pre></td></tr></table></figure><p><strong>断点续跑</strong>：任务失败（机器宕机、App 崩溃未恢复）时，调度中心看到&quot;已上报 passed 的用例&quot;和&quot;计划用例&quot;的差集，把差集重组成新任务重新派发——<strong>不需要重跑已经通过的用例</strong>。这是分布式回归里省钱的关键：一次全量回归中途挂 2 台机器，只重跑 20% 的用例。</p><h2 id="五-规模数据真实压测"><a class="markdownIt-Anchor" href="#五-规模数据真实压测"></a> 五、规模数据（真实压测）</h2><table><thead><tr><th>规模</th><th>全量回归（约 600 用例）</th><th>对比单机</th></tr></thead><tbody><tr><td>单机 8 槽</td><td>3h50m</td><td>1.0×</td></tr><tr><td>3 机 20 槽</td><td>1h18m</td><td>3.0×</td></tr><tr><td>5 机 32 槽</td><td>52m</td><td>4.4×</td></tr></tbody></table><p>注意加速比不是线性的：任务切分粒度（15 分钟装箱）决定了尾部效应——最后一个任务要等它自己跑完。<strong>装箱越均匀，尾部越短</strong>，这也是贪心装箱里按预估时长排序的原因。</p><h2 id="六-踩过的坑"><a class="markdownIt-Anchor" href="#六-踩过的坑"></a> 六、踩过的坑</h2><ol><li><strong>时钟漂移</strong>：5 台机器的系统时间差 2s，导致&quot;用例开始时间&quot;排序全乱——agent 启动时强制 NTP 同步；</li><li><strong>配置包版本</strong>：任务 A 用配置 v12 跑，任务 B 用 v13，结果对比时发现环境不一致——任务元数据里固化配置包 hash，报告头部展示；</li><li><strong>孤儿进程</strong>：任务被 cancel 后 App 进程还活着，占着设备——cancel 语义必须包含&quot;杀进程树 + 卸载代理&quot;，agent 本地兜底超时强杀。</li></ol><h2 id="七-小结"><a class="markdownIt-Anchor" href="#七-小结"></a> 七、小结</h2><p>分布式执行机的设计原则就三条：<strong>worker 无状态</strong>（扩缩容零成本）、<strong>任务粒度 15 分钟</strong>（失败可定位）、<strong>结果逐例上报</strong>（断点可续跑）。不追求 K8s 那种通用编排，测试场景的&quot;设备互斥 + 安装包互斥&quot;用平台聚组解决，简单有效。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;单机跑全量回归要 4 小时，发版窗口只有 90 分钟——分布式执行机不是为了&amp;quot;炫技&amp;quot;，是被发版节奏逼出来的。这篇记录一套轻量分布式测试执行架构：不引入 Kubernetes，N 台 Windows 机器 + 一个调度中心，把 4 小时压进 90 分钟。&lt;
      
    
    </summary>
    
      <category term="分布式" scheme="https://Scofieldsu.github.io/categories/%E5%88%86%E5%B8%83%E5%BC%8F/"/>
    
    
      <category term="分布式" scheme="https://Scofieldsu.github.io/tags/%E5%88%86%E5%B8%83%E5%BC%8F/"/>
    
      <category term="执行机" scheme="https://Scofieldsu.github.io/tags/%E6%89%A7%E8%A1%8C%E6%9C%BA/"/>
    
      <category term="任务调度" scheme="https://Scofieldsu.github.io/tags/%E4%BB%BB%E5%8A%A1%E8%B0%83%E5%BA%A6/"/>
    
  </entry>
  
  <entry>
    <title>用 Mock WebSocket 服务解耦 App 与后端的联调</title>
    <link href="https://Scofieldsu.github.io/2023/09/25/%E7%94%A8MockWebSocket%E6%9C%8D%E5%8A%A1%E8%A7%A3%E8%80%A6App%E4%B8%8E%E5%90%8E%E7%AB%AF%E7%9A%84%E8%81%94%E8%B0%83/"/>
    <id>https://Scofieldsu.github.io/2023/09/25/用MockWebSocket服务解耦App与后端的联调/</id>
    <published>2023-09-25T20:00:00.000Z</published>
    <updated>2026-09-06T03:44:51.439Z</updated>
    
    <content type="html"><![CDATA[<p>App 端测试最大的依赖不是后端接口，而是<strong>长连接</strong>——客服会话、消息推送、状态同步全走 WebSocket。后端一挂或环境一脏，App 用例全陪葬。解法是做一个 <strong>Mock WebSocket 服务</strong>：协议兼容、消息可编程、客户端可批量制造。</p><h2 id="一-协议先行把真实协议固化成契约"><a class="markdownIt-Anchor" href="#一-协议先行把真实协议固化成契约"></a> 一、协议先行：把真实协议固化成契约</h2><p>Mock 的前提是有&quot;真&quot;的参照。第一步不是写代码，而是从真实后端<strong>抓一周的流量</strong>，沉淀出协议契约：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># config/mock_templates.yaml</span></span><br><span class="line"><span class="attr">templates:</span></span><br><span class="line">  <span class="attr">reply-message:</span></span><br><span class="line">    <span class="attr">desc:</span> <span class="string">买家消息回复</span></span><br><span class="line">    <span class="attr">params:</span></span><br><span class="line">      <span class="attr">user_name:</span> &#123; <span class="attr">label:</span> <span class="string">买家昵称</span>, <span class="attr">type:</span> <span class="string">string</span> &#125;</span><br><span class="line">      <span class="attr">conversation_id:</span> &#123; <span class="attr">label:</span> <span class="string">会话ID</span>, <span class="attr">type:</span> <span class="string">string</span> &#125;</span><br><span class="line">      <span class="attr">content:</span> &#123; <span class="attr">label:</span> <span class="string">回复内容</span>, <span class="attr">type:</span> <span class="string">string</span>, <span class="attr">default:</span> <span class="string">&quot;您好，请问需要什么帮助？&quot;</span> &#125;</span><br><span class="line">    <span class="attr">body:</span> <span class="string">|</span></span><br><span class="line"><span class="string">      &#123;&quot;type&quot;:&quot;reply-message&quot;,&quot;request_id&quot;:&quot;&#123;&#123;uuid&#125;&#125;&quot;,&quot;task_id&quot;:&quot;&#123;&#123;task&#125;&#125;&quot;,</span></span><br><span class="line"><span class="string">       &quot;priority&quot;:2,&quot;data&quot;:&#123;&quot;user_name&quot;:&quot;&#123;&#123;user_name&#125;&#125;&quot;,</span></span><br><span class="line"><span class="string">       &quot;conversation_id&quot;:&quot;&#123;&#123;conversation_id&#125;&#125;&quot;,&quot;content&quot;:&quot;&#123;&#123;content&#125;&#125;&quot;&#125;&#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line">  <span class="attr">read-receipt:</span></span><br><span class="line">    <span class="attr">desc:</span> <span class="string">已读回执</span></span><br><span class="line">    <span class="attr">params:</span> &#123; <span class="attr">conversation_id:</span> &#123; <span class="attr">type:</span> <span class="string">string</span> &#125; &#125;</span><br><span class="line">    <span class="attr">body:</span> <span class="string">&#x27;&#123;&quot;type&quot;:&quot;read-receipt&quot;,&quot;data&quot;:&#123;&quot;conversation_id&quot;:&quot;<span class="template-variable">&#123;&#123;conversation_id&#125;&#125;</span>&quot;&#125;&#125;&#x27;</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">typing:</span></span><br><span class="line">    <span class="attr">desc:</span> <span class="string">对方正在输入</span></span><br><span class="line">    <span class="attr">params:</span> &#123; <span class="attr">user_name:</span> &#123; <span class="attr">type:</span> <span class="string">string</span> &#125; &#125;</span><br><span class="line">    <span class="attr">body:</span> <span class="string">&#x27;&#123;&quot;type&quot;:&quot;typing&quot;,&quot;data&quot;:&#123;&quot;user_name&quot;:&quot;<span class="template-variable">&#123;&#123;user_name&#125;&#125;</span>&quot;&#125;&#125;&#x27;</span></span><br></pre></td></tr></table></figure><p>模板 + 参数占位符 + <code>&#123;&#123;uuid&#125;&#125;</code>/<code>&#123;&#123;task&#125;&#125;</code> 内置变量，Web 控制台填参数即出报文——<strong>Mock 的可编程性来自模板，不是来自硬编码</strong>。</p><h2 id="二-服务设计广播-定向-录制回放"><a class="markdownIt-Anchor" href="#二-服务设计广播-定向-录制回放"></a> 二、服务设计：广播 + 定向 + 录制回放</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MockWsServer</span>:</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;核心就三件事：管理连接、定向下发、广播、记录流水&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self</span>):</span></span><br><span class="line">        self.clients: <span class="built_in">dict</span>[<span class="built_in">str</span>, WebSocket] = &#123;&#125;   <span class="comment"># conn_id -&gt; ws</span></span><br><span class="line">        self.history: deque[<span class="built_in">dict</span>] = deque(maxlen=<span class="number">2000</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">async</span> <span class="function"><span class="keyword">def</span> <span class="title">broadcast</span>(<span class="params">self, payload: <span class="built_in">dict</span></span>):</span></span><br><span class="line">        <span class="string">&quot;&quot;&quot;广播给所有活跃连接（模拟服务端主动推送）&quot;&quot;&quot;</span></span><br><span class="line">        <span class="keyword">for</span> conn <span class="keyword">in</span> self.clients.values():</span><br><span class="line">            <span class="keyword">await</span> conn.send_json(payload)</span><br><span class="line">        self.history.append(&#123;<span class="string">&quot;dir&quot;</span>: <span class="string">&quot;s2c&quot;</span>, <span class="string">&quot;conn&quot;</span>: <span class="string">&quot;*&quot;</span>, **payload&#125;)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">async</span> <span class="function"><span class="keyword">def</span> <span class="title">send_to</span>(<span class="params">self, conn_id: <span class="built_in">str</span>, payload: <span class="built_in">dict</span></span>):</span></span><br><span class="line">        <span class="string">&quot;&quot;&quot;定向下发（模拟&quot;给某买家回消息&quot;）&quot;&quot;&quot;</span></span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">async</span> <span class="function"><span class="keyword">def</span> <span class="title">on_client_message</span>(<span class="params">self, conn_id: <span class="built_in">str</span>, payload: <span class="built_in">dict</span></span>):</span></span><br><span class="line">        <span class="string">&quot;&quot;&quot;c2s 方向：App 发来的帧，记录 + 按规则自动应答&quot;&quot;&quot;</span></span><br><span class="line">        self.history.append(&#123;<span class="string">&quot;dir&quot;</span>: <span class="string">&quot;c2s&quot;</span>, <span class="string">&quot;conn&quot;</span>: conn_id, **payload&#125;)</span><br><span class="line">        <span class="keyword">await</span> self._auto_reply(conn_id, payload)</span><br></pre></td></tr></table></figure><p>三个设计点：</p><ol><li><strong>自动应答规则表</strong>：App 发 <code>login</code> 帧必须回 <code>login-ok</code>，否则后续全断。把这类&quot;握手型应答&quot;写成规则表自动触发，模板下发留给业务帧——Mock 既要&quot;像服务端&quot;，又要&quot;可控&quot;；</li><li><strong>双方向流水</strong>（c2s/s2c）全部进环形缓冲，Web 控制台实时滚动展示——联调时&quot;App 到底发了什么&quot;一眼可见，比在 App 里抓包快十倍；</li><li><strong>客户端可批量制造</strong>：Mock 客户端脚本可以一次连 20 个&quot;假买家&quot;，模拟群发消息、多会话并发，这是真实环境造不出的数据。</li></ol><h2 id="三-与-ui-自动化联动"><a class="markdownIt-Anchor" href="#三-与-ui-自动化联动"></a> 三、与 UI 自动化联动</h2><p>Mock 服务的价值在&quot;场景编排&quot;：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@pytest.fixture(<span class="params">scope=<span class="string">&quot;session&quot;</span></span>)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">mock</span>():</span></span><br><span class="line">    server = MockWsClient(base_url=<span class="string">&quot;http://127.0.0.1:8900&quot;</span>)</span><br><span class="line">    <span class="keyword">yield</span> server</span><br><span class="line">    server.reset()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test_unread_badge</span>(<span class="params">mock, app_driver</span>):</span></span><br><span class="line">    mock.connect_fake_buyer(<span class="string">&quot;buyer_001&quot;</span>)</span><br><span class="line">    open_chat_list(app_driver)</span><br><span class="line">    mock.send_to(app_conn, make(<span class="string">&quot;reply-message&quot;</span>, user_name=<span class="string">&quot;buyer_001&quot;</span>,</span><br><span class="line">                                content=<span class="string">&quot;在吗？订单什么时候发货？&quot;</span>))</span><br><span class="line">    <span class="comment"># 断言：角标 +1、列表顶部出现新会话、红点样式</span></span><br><span class="line">    <span class="keyword">assert</span> unread_badge(app_driver) == <span class="number">1</span></span><br></pre></td></tr></table></figure><p><strong>测试矩阵</strong>：同一套 UI 用例 × 三类服务端行为（正常推送 / 延迟推送 / 乱序推送），乱序推送是真实网络下 WebSocket 帧乱序的模拟——<code>typing</code> 事件晚于 <code>reply-message</code> 到达时，UI 不能闪烁、不能把&quot;正在输入&quot;残留到消息之后。这种 case 只在 Mock 环境造得出。</p><h2 id="四-为什么不用-wiremock-现成工具"><a class="markdownIt-Anchor" href="#四-为什么不用-wiremock-现成工具"></a> 四、为什么不用 WireMock / 现成工具</h2><table><thead><tr><th>需求</th><th>现成工具</th><th>自研 Mock WS</th></tr></thead><tbody><tr><td>HTTP 接口 Mock</td><td>✅ 强</td><td>不需要</td></tr><tr><td>WS 帧级控制（延迟/乱序/断连）</td><td>❌ 基本没有</td><td>✅</td></tr><tr><td>业务模板参数化</td><td>❌</td><td>✅</td></tr><tr><td>与 UI 用例 fixture 级联动</td><td>⚠️ 要自己桥</td><td>✅ 原生 Python</td></tr></tbody></table><p>现成工具解决 80% 的&quot;接口 Mock&quot;，但 <strong>WS 的帧级时序控制是自动化测试的刚需</strong>，这部分只能自己写——好在核心逻辑（连接管理 + 广播 + 流水）不到 300 行，Web 控制台只是它的一个客户端。</p><h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2><p>Mock WebSocket 服务让 App 测试从&quot;依赖环境&quot;变成&quot;编排环境&quot;：协议契约 YAML 化、帧时序可编程、双方向流水透明。联调效率的提升是量级的——过去一个&quot;消息角标不更新&quot;的排查要拉后端抓包两小时，现在 Mock 里重放一遍 5 分钟定位。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;App 端测试最大的依赖不是后端接口，而是&lt;strong&gt;长连接&lt;/strong&gt;——客服会话、消息推送、状态同步全走 WebSocket。后端一挂或环境一脏，App 用例全陪葬。解法是做一个 &lt;strong&gt;Mock WebSocket 服务&lt;/strong&gt;：协议兼容、
      
    
    </summary>
    
      <category term="自动化测试" scheme="https://Scofieldsu.github.io/categories/%E8%87%AA%E5%8A%A8%E5%8C%96%E6%B5%8B%E8%AF%95/"/>
    
    
      <category term="Mock" scheme="https://Scofieldsu.github.io/tags/Mock/"/>
    
      <category term="WebSocket" scheme="https://Scofieldsu.github.io/tags/WebSocket/"/>
    
      <category term="联调" scheme="https://Scofieldsu.github.io/tags/%E8%81%94%E8%B0%83/"/>
    
  </entry>
  
  <entry>
    <title>Flask 测试平台后端架构：模块注册、配置与运行时上下文</title>
    <link href="https://Scofieldsu.github.io/2023/06/12/Flask%E6%B5%8B%E8%AF%95%E5%B9%B3%E5%8F%B0%E5%90%8E%E7%AB%AF%E6%9E%B6%E6%9E%84%EF%BC%9A%E6%A8%A1%E5%9D%97%E6%B3%A8%E5%86%8C%E3%80%81%E9%85%8D%E7%BD%AE%E4%B8%8E%E8%BF%90%E8%A1%8C%E6%97%B6%E4%B8%8A%E4%B8%8B%E6%96%87/"/>
    <id>https://Scofieldsu.github.io/2023/06/12/Flask测试平台后端架构：模块注册、配置与运行时上下文/</id>
    <published>2023-06-12T21:30:00.000Z</published>
    <updated>2026-09-06T03:44:51.437Z</updated>
    
    <content type="html"><![CDATA[<p>用 Flask 搭了一个自动化测试平台（Web 控制台 + 执行引擎 + 实时日志），踩过的架构坑值得复盘。核心矛盾：<strong>Flask 的 request 上下文 vs 平台的后台线程</strong>——任务执行、定时调度、SSE 推流都活在 request 之外，怎么让所有代码取到同一份配置和状态？</p><h2 id="一-应用工厂-模块自动注册"><a class="markdownIt-Anchor" href="#一-应用工厂-模块自动注册"></a> 一、应用工厂 + 模块自动注册</h2><p>平台有十几个功能模块（任务、机器、代理、报告……），不想让 <code>app.py</code> 长成几百行：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># server/__init__.py</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">create_app</span>(<span class="params">app_config=<span class="literal">None</span></span>):</span></span><br><span class="line">    app = Flask(__name__)</span><br><span class="line">    app.config.from_mapping(</span><br><span class="line">        PROJECT_ROOT=<span class="built_in">str</span>(app_config.paths.project_root),</span><br><span class="line">        CURRENT_ENV=app_config.current_env,</span><br><span class="line">        WEB_USERS=<span class="built_in">list</span>(app_config.web_users.users),</span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line">    <span class="keyword">from</span> .modules <span class="keyword">import</span> register_all</span><br><span class="line">    register_all(app)        <span class="comment"># 自动扫描 modules/ 注册所有 Blueprint</span></span><br><span class="line">    <span class="keyword">return</span> app</span><br></pre></td></tr></table></figure><p>模块约定：每个模块一个包，<code>__init__.py</code> 里暴露 <code>bp = Blueprint(&quot;xxx&quot;, __name__, url_prefix=&quot;/api/xxx&quot;)</code>，注册器扫描 <code>server/modules/</code> 下所有带 <code>bp</code> 的包统一 <code>app.register_blueprint</code>。<strong>新增模块 = 新建一个目录</strong>，不改任何既有文件。</p><h2 id="二-配置yaml-为源环境变量可覆盖"><a class="markdownIt-Anchor" href="#二-配置yaml-为源环境变量可覆盖"></a> 二、配置：YAML 为源，环境变量可覆盖</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@dataclass</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ServerConfig</span>(<span class="params">BaseConfig</span>):</span></span><br><span class="line">    port: <span class="built_in">int</span> = <span class="number">6001</span></span><br><span class="line">    host: <span class="built_in">str</span> = <span class="string">&quot;0.0.0.0&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@classmethod</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">from_env</span>(<span class="params">cls, prefix=<span class="string">&quot;&quot;</span></span>):</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;AT_WEB_SERVER_PORT 这类环境变量覆盖，部署时不改文件&quot;&quot;&quot;</span></span><br><span class="line">    ...</span><br></pre></td></tr></table></figure><p>三层优先级：<strong>环境变量 &gt; 本地 YAML &gt; 代码默认值</strong>。测试平台的账号密码、目标环境 URL 这类值，部署差异全走环境变量，YAML 只放&quot;项目默认值&quot;，仓库里永远是安全的演示配置。</p><h2 id="三-runtime-模块request-之外的单例上下文"><a class="markdownIt-Anchor" href="#三-runtime-模块request-之外的单例上下文"></a> 三、runtime 模块：request 之外的单例上下文</h2><p>这是整个架构里最关键的一个文件。Flask 的 <code>current_app</code> 只在请求上下文里可用，而任务执行线程、SSE 推送线程、定时调度线程全在 request 之外：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># server/runtime.py</span></span><br><span class="line">_lock = threading.Lock()</span><br><span class="line">_root: Path | <span class="literal">None</span> = <span class="literal">None</span></span><br><span class="line">_env: <span class="built_in">str</span> = <span class="string">&quot;dev&quot;</span></span><br><span class="line">_platforms: <span class="built_in">list</span>[<span class="built_in">dict</span>] = []</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">init</span>(<span class="params">root, env, platforms</span>):</span></span><br><span class="line">    <span class="keyword">global</span> _root, _env, _platforms</span><br><span class="line">    <span class="keyword">with</span> _lock:</span><br><span class="line">        _root, _env, _platforms = Path(root), env <span class="keyword">or</span> <span class="string">&quot;dev&quot;</span>, <span class="built_in">list</span>(platforms)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">current_env</span>() -&gt; <span class="built_in">str</span>:</span></span><br><span class="line">    <span class="keyword">return</span> _env</span><br></pre></td></tr></table></figure><p><code>create_app</code> 时 <code>runtime.init(...)</code> 一次，之后<strong>任何线程</strong>都通过 <code>runtime.current_env()</code> / <code>runtime.project_root()</code> 取值，不碰 <code>current_app</code>。切换环境时调 <code>runtime.set_env(&quot;prod&quot;)</code>，全局立即生效。</p><p><strong>反模式对照</strong>：一开始把环境写死在 <code>current_app.config</code> 里，结果任务线程里一用就抛 <code>RuntimeError: Working outside of application context</code>——这类&quot;上下文越界&quot;bug 在 Flask + 线程的组合里极其常见，runtime 单例是标准解法。</p><h2 id="四-统一鉴权before_request-一把闸"><a class="markdownIt-Anchor" href="#四-统一鉴权before_request-一把闸"></a> 四、统一鉴权：before_request 一把闸</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@app.before_request</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_auth_guard</span>():</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> request.path.startswith(<span class="string">&quot;/api/&quot;</span>):</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    <span class="keyword">if</span> request.path == <span class="string">&quot;/api/auth/login&quot;</span>:</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> is_authenticated():          <span class="comment"># session cookie 校验</span></span><br><span class="line">        <span class="keyword">return</span> jsonify(&#123;<span class="string">&quot;error&quot;</span>: <span class="string">&quot;unauthorized&quot;</span>&#125;), <span class="number">401</span></span><br></pre></td></tr></table></figure><p>所有 API 默认拒绝，白名单只有登录接口。SPA 页面本身不走鉴权（静态文件），由前端路由守卫 + API 401 双保险。测试平台的账号来自配置而非数据库，<code>before_request</code> 里查字典即可，简单且零依赖。</p><h2 id="五-sse-实时日志generator-注册表"><a class="markdownIt-Anchor" href="#五-sse-实时日志generator-注册表"></a> 五、SSE 实时日志：generator + 注册表</h2><p>任务执行日志要实时推到浏览器，HTTP 长轮询太糙，用 SSE：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@bp.route(<span class="params"><span class="string">&quot;/ui/events&quot;</span></span>)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">events</span>():</span></span><br><span class="line">    q = live_log.subscribe()           <span class="comment"># 注册表分配一个队列</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">stream</span>():</span></span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">                msg = q.get(timeout=<span class="number">15</span>)</span><br><span class="line">                <span class="keyword">if</span> msg <span class="keyword">is</span> <span class="literal">None</span>:        <span class="comment"># 心跳超时</span></span><br><span class="line">                    <span class="keyword">yield</span> <span class="string">&quot;: ping\n\n&quot;</span></span><br><span class="line">                <span class="keyword">else</span>:</span><br><span class="line">                    <span class="keyword">yield</span> <span class="string">f&quot;data: <span class="subst">&#123;json.dumps(msg)&#125;</span>\n\n&quot;</span></span><br><span class="line">        <span class="keyword">finally</span>:</span><br><span class="line">            live_log.unsubscribe(q)</span><br><span class="line">    <span class="keyword">return</span> Response(stream(), mimetype=<span class="string">&quot;text/event-stream&quot;</span>)</span><br></pre></td></tr></table></figure><p><code>live_log</code> 是一个进程内注册表（任务 ID → 队列列表），执行引擎在后台线程里 <code>live_log.emit(task_id, level, text)</code>，所有订阅者（SSE 连接、终端）都能收到。<strong>单进程足够</strong>——测试平台是内网工具，不追求横向扩展，用进程内队列换掉了 Redis 的复杂度。</p><h2 id="六-小结"><a class="markdownIt-Anchor" href="#六-小结"></a> 六、小结</h2><p>Flask 做平台后端，四个决定决定了后续所有开发的顺逆：<strong>工厂 + 模块自注册</strong>（扩展不改核心）、<strong>配置分层</strong>（环境差异不落盘）、<strong>runtime 单例</strong>（线程安全上下文）、<strong>进程内注册表</strong>（实时推送不过度设计）。Flask 的&quot;小&quot;在这里不是缺点，恰恰是可控性。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;用 Flask 搭了一个自动化测试平台（Web 控制台 + 执行引擎 + 实时日志），踩过的架构坑值得复盘。核心矛盾：&lt;strong&gt;Flask 的 request 上下文 vs 平台的后台线程&lt;/strong&gt;——任务执行、定时调度、SSE 推流都活在 request 之
      
    
    </summary>
    
      <category term="后端开发" scheme="https://Scofieldsu.github.io/categories/%E5%90%8E%E7%AB%AF%E5%BC%80%E5%8F%91/"/>
    
    
      <category term="Flask" scheme="https://Scofieldsu.github.io/tags/Flask/"/>
    
      <category term="架构" scheme="https://Scofieldsu.github.io/tags/%E6%9E%B6%E6%9E%84/"/>
    
      <category term="Python" scheme="https://Scofieldsu.github.io/tags/Python/"/>
    
  </entry>
  
  <entry>
    <title>PC 端 UI 自动化：pywinauto 控件树实战笔记</title>
    <link href="https://Scofieldsu.github.io/2023/02/18/PC%E7%AB%AFUI%E8%87%AA%E5%8A%A8%E5%8C%96%EF%BC%9Apywinauto%E6%8E%A7%E4%BB%B6%E6%A0%91%E5%AE%9E%E6%88%98%E7%AC%94%E8%AE%B0/"/>
    <id>https://Scofieldsu.github.io/2023/02/18/PC端UI自动化：pywinauto控件树实战笔记/</id>
    <published>2023-02-18T20:10:00.000Z</published>
    <updated>2026-09-06T03:44:51.437Z</updated>
    
    <content type="html"><![CDATA[<p>App 自动化做熟之后回头做 PC 端，最大的感受是：Windows 桌面比移动端<strong>更不标准</strong>——同一个&quot;对话框&quot;，Qt、WPF、Win32、Electron 各长各的。pywinauto 是覆盖最广的选择，这篇记录控件树分析和实战中最重要的几手。</p><h2 id="一-两种-backend先分清楚"><a class="markdownIt-Anchor" href="#一-两种-backend先分清楚"></a> 一、两种 backend，先分清楚</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> pywinauto <span class="keyword">import</span> Application</span><br><span class="line"></span><br><span class="line"><span class="comment"># UIA backend：支持 Win10+ 的几乎所有框架（WPF/Qt/UWP/Electron 部分）</span></span><br><span class="line">app = Application(backend=<span class="string">&quot;uia&quot;</span>).connect(title_re=<span class="string">&quot;.*订单管理.*&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># win32 backend：经典 Win32 控件，更快更稳，但只认传统消息循环</span></span><br><span class="line">app = Application(backend=<span class="string">&quot;win32&quot;</span>).connect(title=<span class="string">&quot;计算器&quot;</span>)</span><br></pre></td></tr></table></figure><p><strong>选型经验</strong>：</p><ul><li>新框架（Qt5+、WPF）一律先试 <code>uia</code>；</li><li>老系统工具类程序（控制面板、传统 MDI）用 <code>win32</code> 更快；</li><li>Electron 应用：UIA 只能看到窗口级结构，<strong>内部 DOM 要走 CDP</strong>（<code>--remote-debugging-port</code>），别硬刚控件树。</li></ul><h2 id="二-控件树分析dump-先行拒绝盲写选择器"><a class="markdownIt-Anchor" href="#二-控件树分析dump-先行拒绝盲写选择器"></a> 二、控件树分析：dump 先行，拒绝盲写选择器</h2><p>pywinauto 最大的价值是把控件树变成可遍历的 Python 对象：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1) 全树 dump 到文件，先看结构再写脚本</span></span><br><span class="line">dlg.print_control_identifiers(depth=<span class="number">8</span>, filename=<span class="string">&quot;tree.txt&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2) 按属性定位（推荐组合：auto_id 优先，text 兜底）</span></span><br><span class="line">send_btn = dlg.child_window(auto_id=<span class="string">&quot;btnSend&quot;</span>).wait(<span class="string">&quot;visible ready&quot;</span>, timeout=<span class="number">10</span>)</span><br><span class="line">send_btn.click_input()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3) 表格控件：DataGrid/ListView 的行列取值</span></span><br><span class="line">grid = dlg.child_window(auto_id=<span class="string">&quot;dataGrid&quot;</span>).wrapper_object()</span><br><span class="line">grid.select([<span class="number">0</span>, <span class="number">2</span>])                    <span class="comment"># 选中第 3 行第 1 列</span></span><br><span class="line">value = grid.get_item((<span class="number">2</span>, <span class="number">0</span>)).text()</span><br></pre></td></tr></table></figure><p><strong>实战约定</strong>：控件定位写选择器前，先 dump 出 auto_id 清单和开发对齐——桌面应用的 auto_id 往往藏在代码的 <code>ObjectName</code>/<code>AutomationProperties.AutomationId</code> 里，和 App 端的 resource-id 是同一个道理：<strong>有约定就永远有稳定定位</strong>。</p><h2 id="三-虚拟列表滚动加载的坑"><a class="markdownIt-Anchor" href="#三-虚拟列表滚动加载的坑"></a> 三、虚拟列表：滚动加载的坑</h2><p>长列表（万行级别）在 UIA 里是<strong>虚拟化的</strong>——只渲染可见区域，<code>get_item(5000)</code> 直接报&quot;元素不在可视区&quot;。解法：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">find_row</span>(<span class="params">grid, text, max_scrolls=<span class="number">200</span></span>):</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;二分滚动定位：先粗扫再精扫，比线性滚快一个量级&quot;&quot;&quot;</span></span><br><span class="line">    <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(max_scrolls):</span><br><span class="line">        visible = grid.visible_items()</span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">any</span>(text <span class="keyword">in</span> (it.text() <span class="keyword">or</span> <span class="string">&quot;&quot;</span>) <span class="keyword">for</span> it <span class="keyword">in</span> visible):</span><br><span class="line">            <span class="keyword">return</span> _click_match(grid, visible, text)</span><br><span class="line">        <span class="keyword">if</span> _reached_bottom(grid):</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line">        grid.scroll_down(<span class="number">30</span>)   <span class="comment"># 每次滚 30 行</span></span><br><span class="line">    <span class="keyword">raise</span> TimeoutError(<span class="string">f&quot;未找到包含 <span class="subst">&#123;text!r&#125;</span> 的行&quot;</span>)</span><br></pre></td></tr></table></figure><p>要点：</p><ul><li><strong>滚动步长要自适应</strong>：一次滚太多会跳过目标（虚拟化渲染有缓冲），30~50 行是安全步长；</li><li>找不到先确认&quot;到底了没有&quot;再抛超时，区分&quot;不存在&quot;和&quot;没滚到&quot;；</li><li>定位到行之后，<strong>先滚动到可见再点击</strong>，<code>click_input()</code> 要求元素真实可见。</li></ul><h2 id="四-多窗口子窗口管理"><a class="markdownIt-Anchor" href="#四-多窗口子窗口管理"></a> 四、多窗口/子窗口管理</h2><p>桌面应用动辄主窗口 + 弹窗 + 子面板：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 弹窗出现后接管</span></span><br><span class="line"><span class="keyword">with</span> app.window(title_re=<span class="string">&quot;确认.*&quot;</span>) <span class="keyword">as</span> confirm:</span><br><span class="line">    confirm.child_window(title=<span class="string">&quot;确定&quot;</span>).click_input()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 子面板切换：TabControl 要激活页签后控件才可交互</span></span><br><span class="line">tab = dlg.child_window(auto_id=<span class="string">&quot;tabOrders&quot;</span>)</span><br><span class="line">tab.select(<span class="number">2</span>)                      <span class="comment"># 切到第 3 个页签</span></span><br><span class="line">tab.wait(<span class="string">&quot;visible ready&quot;</span>)</span><br></pre></td></tr></table></figure><p><code>with</code> 上下文管理窗口生命周期是 pywinauto 最省心的设计：弹窗关掉、窗口销毁都自动处理，不会留下野引用。</p><h2 id="五-稳定性桌面版的三板斧"><a class="markdownIt-Anchor" href="#五-稳定性桌面版的三板斧"></a> 五、稳定性：桌面版的三板斧</h2><p>和 App 端一脉相承：</p><ol><li><strong>等待</strong>：<code>wait(&quot;visible ready&quot;)</code> 永远带上 timeout，禁止 <code>time.sleep</code>；</li><li><strong>焦点</strong>：<code>click_input()</code> 是真实鼠标点击，受焦点/遮挡影响——自动化期间保证目标窗口在最前（<code>restore()</code> + <code>set_focus()</code>），被其他窗口遮挡时 <code>click_input</code> 会点错地方；</li><li><strong>失败现场</strong>：失败时 <code>win32gui</code> 截屏 + <code>print_control_identifiers</code> 导出当前树，两个产物一起归档，定位&quot;控件树变了还是时序问题&quot;一目了然。</li></ol><h2 id="六-小结"><a class="markdownIt-Anchor" href="#六-小结"></a> 六、小结</h2><p>PC 端自动化的核心是<strong>先理解控件树，再写选择器</strong>：backend 选对、auto_id 有约定、虚拟列表会滚动、窗口生命周期用 with 管。做到这四点，pywinauto 的脚本稳定性和 App 端可以打平。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;App 自动化做熟之后回头做 PC 端，最大的感受是：Windows 桌面比移动端&lt;strong&gt;更不标准&lt;/strong&gt;——同一个&amp;quot;对话框&amp;quot;，Qt、WPF、Win32、Electron 各长各的。pywinauto 是覆盖最广的选择，这篇记录控件树分
      
    
    </summary>
    
      <category term="PC端自动化" scheme="https://Scofieldsu.github.io/categories/PC%E7%AB%AF%E8%87%AA%E5%8A%A8%E5%8C%96/"/>
    
    
      <category term="pywinauto" scheme="https://Scofieldsu.github.io/tags/pywinauto/"/>
    
      <category term="桌面自动化" scheme="https://Scofieldsu.github.io/tags/%E6%A1%8C%E9%9D%A2%E8%87%AA%E5%8A%A8%E5%8C%96/"/>
    
      <category term="Windows" scheme="https://Scofieldsu.github.io/tags/Windows/"/>
    
  </entry>
  
  <entry>
    <title>弱网测试怎么做：用 mitmproxy 模拟真实网络劣化</title>
    <link href="https://Scofieldsu.github.io/2022/11/08/%E5%BC%B1%E7%BD%91%E6%B5%8B%E8%AF%95%EF%BC%9A%E7%94%A8mitmproxy%E6%A8%A1%E6%8B%9F%E7%9C%9F%E5%AE%9E%E7%BD%91%E7%BB%9C%E5%8A%A3%E5%8C%96/"/>
    <id>https://Scofieldsu.github.io/2022/11/08/弱网测试：用mitmproxy模拟真实网络劣化/</id>
    <published>2022-11-08T19:40:00.000Z</published>
    <updated>2026-09-06T03:44:51.439Z</updated>
    
    <content type="html"><![CDATA[<p>用户反馈里最经典的一类：“为什么你们测试都过了，我这边 App 一直转圈？”——答案通常是<strong>没人测过弱网</strong>。这篇文章介绍一套基于 mitmproxy 的弱网模拟方案：不依赖真机、可脚本化、可进 CI。</p><h2 id="一-为什么用-mitmproxy-而不是系统级工具"><a class="markdownIt-Anchor" href="#一-为什么用-mitmproxy-而不是系统级工具"></a> 一、为什么用 mitmproxy 而不是系统级工具</h2><table><thead><tr><th>方案</th><th>优点</th><th>缺点</th></tr></thead><tbody><tr><td>系统工具（Clumsy/ATC）</td><td>傻瓜式</td><td>绑死 GUI，没法进 CI，参数难精确</td></tr><tr><td>交换机 QoS</td><td>真实</td><td>成本高、粒度粗、要机房配合</td></tr><tr><td><strong>mitmproxy add-on</strong></td><td>可编程、参数精确、CI 友好</td><td>只覆盖 TCP 层可控部分，DNS/RTT 要配合</td></tr></tbody></table><p>核心思路：设备流量经过 mitmproxy 代理，<strong>用 add-on 在请求/响应路径上插入可控的延迟、限速、丢包</strong>，全部用 YAML 声明场景，脚本按场景启动。</p><h2 id="二-弱网场景模型"><a class="markdownIt-Anchor" href="#二-弱网场景模型"></a> 二、弱网场景模型</h2><p>把&quot;弱网&quot;拆成四个独立维度，每个维度单独可调：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># config/proxy/weaknet_scenarios.yaml</span></span><br><span class="line"><span class="attr">wifi_bad:</span>            <span class="comment"># 电梯/地铁 WiFi</span></span><br><span class="line">  <span class="attr">latency_ms:</span> <span class="number">300</span>    <span class="comment"># 单程延迟</span></span><br><span class="line">  <span class="attr">bandwidth_kbps:</span> <span class="number">500</span></span><br><span class="line">  <span class="attr">loss_pct:</span> <span class="number">2</span>        <span class="comment"># 丢包率</span></span><br><span class="line"><span class="attr">cell_3g:</span>             <span class="comment"># 3G 时代体验</span></span><br><span class="line">  <span class="attr">latency_ms:</span> <span class="number">800</span></span><br><span class="line">  <span class="attr">bandwidth_kbps:</span> <span class="number">2000</span></span><br><span class="line">  <span class="attr">loss_pct:</span> <span class="number">5</span></span><br><span class="line"><span class="attr">flaky:</span>               <span class="comment"># 时断时续（最难测，也最有价值）</span></span><br><span class="line">  <span class="attr">latency_ms:</span> <span class="number">200</span></span><br><span class="line">  <span class="attr">bandwidth_kbps:</span> <span class="number">1000</span></span><br><span class="line">  <span class="attr">loss_pct:</span> <span class="number">15</span></span><br><span class="line">  <span class="attr">stall_every:</span> <span class="number">8</span>     <span class="comment"># 每 8 个请求卡住一次</span></span><br><span class="line">  <span class="attr">stall_ms:</span> <span class="number">5000</span></span><br></pre></td></tr></table></figure><p><strong>经验值</strong>：<code>flaky</code>（间歇性中断）场景抓到的 bug 数量是稳定弱网的 3 倍以上——用户手机的实际网络就是&quot;时好时坏&quot;，而不是&quot;稳定很慢&quot;。</p><h2 id="三-add-on-实现核心"><a class="markdownIt-Anchor" href="#三-add-on-实现核心"></a> 三、add-on 实现核心</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># rpa_tools/proxy_manager.py（节选）</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">WeaknetAddon</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, scenario: <span class="built_in">dict</span></span>):</span></span><br><span class="line">        self.scenario = scenario</span><br><span class="line">        self.counter = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">request</span>(<span class="params">self, flow</span>):</span></span><br><span class="line">        s = self.scenario</span><br><span class="line">        <span class="comment"># 1) 延迟：在请求侧注入（模拟 RTT 的一半）</span></span><br><span class="line">        time.sleep(s[<span class="string">&quot;latency_ms&quot;</span>] / <span class="number">1000</span>)</span><br><span class="line">        <span class="comment"># 2) 丢包：按概率直接掐断</span></span><br><span class="line">        <span class="keyword">if</span> random.random() * <span class="number">100</span> &lt; s.get(<span class="string">&quot;loss_pct&quot;</span>, <span class="number">0</span>):</span><br><span class="line">            flow.error = http.Response(<span class="number">408</span>, <span class="string">&quot;Timeout&quot;</span>, <span class="string">b&quot;&quot;</span>)</span><br><span class="line">        <span class="comment"># 3) 间歇卡顿</span></span><br><span class="line">        self.counter += <span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> s.get(<span class="string">&quot;stall_every&quot;</span>) <span class="keyword">and</span> self.counter % s[<span class="string">&quot;stall_every&quot;</span>] == <span class="number">0</span>:</span><br><span class="line">            time.sleep(s.get(<span class="string">&quot;stall_ms&quot;</span>, <span class="number">0</span>) / <span class="number">1000</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">responseheaders</span>(<span class="params">self, flow</span>):</span></span><br><span class="line">        <span class="comment"># 4) 限速：按带宽把响应切成小片慢慢吐</span></span><br><span class="line">        self._throttle(flow)</span><br></pre></td></tr></table></figure><p>限速不是 <code>time.sleep</code> 一刀切，而是<strong>按 chunk 发送</strong>：<code>bandwidth_kbps</code> 换算成每 KB 的间隔，响应流式分片释放，这样前端能观察到&quot;转圈越来越长&quot;的真实过程，而不是一开始就等很久。</p><h2 id="四-和-ui-自动化联动"><a class="markdownIt-Anchor" href="#四-和-ui-自动化联动"></a> 四、和 UI 自动化联动</h2><p>弱网不是&quot;代理自己跑&quot;，而是<strong>给 UI 用例套一层场景</strong>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@pytest.fixture(<span class="params">scope=<span class="string">&quot;session&quot;</span></span>)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">weaknet</span>():</span></span><br><span class="line">    mgr = ProxyManager()</span><br><span class="line">    <span class="keyword">yield</span> mgr   <span class="comment"># mgr.apply(&quot;flaky&quot;) / mgr.reset()</span></span><br><span class="line">    mgr.stop()</span><br><span class="line"></span><br><span class="line"><span class="meta">@pytest.mark.weaknet(<span class="params"><span class="string">&quot;flaky&quot;</span></span>)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test_login_under_flaky_network</span>(<span class="params">driver, weaknet</span>):</span></span><br><span class="line">    weaknet.apply(<span class="string">&quot;flaky&quot;</span>)</span><br><span class="line">    open_login_page(driver)</span><br><span class="line">    submit_login(driver)</span><br><span class="line">    <span class="comment"># 关键断言：允许失败重试，但必须有 loading 态 + 明确的失败文案</span></span><br><span class="line">    <span class="keyword">assert</span> wait_loading_or_error(driver, timeout=<span class="number">30</span>)</span><br></pre></td></tr></table></figure><p><code>pytest.ini</code> 里注册 <code>weaknet</code> 标记，CI 的 <code>api</code> 档流水线里加一条 <code>pytest -m weaknet</code> 的 job，<strong>每个发版前自动过一遍全部弱网场景</strong>。</p><h2 id="五-断言什么才算弱网表现合格"><a class="markdownIt-Anchor" href="#五-断言什么才算弱网表现合格"></a> 五、断言什么才算&quot;弱网表现合格&quot;</h2><p>弱网下的测试重点<strong>不是功能能不能完成</strong>（那是正常网络的测试职责），而是：</p><ol><li><strong>有 loading 反馈</strong>：任何超过 1s 的操作必须有可见的等待指示；</li><li><strong>失败有出口</strong>：超时/断网必须有重试按钮或明确提示，禁止白屏死等；</li><li><strong>幂等</strong>：网络抖动导致重复提交时，服务端不能创建两条订单——这条必须用接口层断言兜住；</li><li><strong>恢复即续</strong>：网络恢复后，之前&quot;卡住&quot;的页面状态要能自然续上，而不是整页白屏要求重进。</li></ol><h2 id="六-小结"><a class="markdownIt-Anchor" href="#六-小结"></a> 六、小结</h2><p>弱网测试的落地三件套：<strong>场景 YAML 化、add-on 可编程、标记进 CI</strong>。工具选型上 mitmproxy 的 Python add-on 生态让&quot;网络条件&quot;变成了和普通测试数据一样可以参数化的东西——这是它比 GUI 工具强的根本原因。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;用户反馈里最经典的一类：“为什么你们测试都过了，我这边 App 一直转圈？”——答案通常是&lt;strong&gt;没人测过弱网&lt;/strong&gt;。这篇文章介绍一套基于 mitmproxy 的弱网模拟方案：不依赖真机、可脚本化、可进 CI。&lt;/p&gt;
&lt;h2 id=&quot;一-为什么用-mi
      
    
    </summary>
    
      <category term="自动化测试" scheme="https://Scofieldsu.github.io/categories/%E8%87%AA%E5%8A%A8%E5%8C%96%E6%B5%8B%E8%AF%95/"/>
    
    
      <category term="弱网测试" scheme="https://Scofieldsu.github.io/tags/%E5%BC%B1%E7%BD%91%E6%B5%8B%E8%AF%95/"/>
    
      <category term="mitmproxy" scheme="https://Scofieldsu.github.io/tags/mitmproxy/"/>
    
      <category term="网络模拟" scheme="https://Scofieldsu.github.io/tags/%E7%BD%91%E7%BB%9C%E6%A8%A1%E6%8B%9F/"/>
    
  </entry>
  
  <entry>
    <title>pytest 实战：从单文件脚本到可维护的测试工程</title>
    <link href="https://Scofieldsu.github.io/2022/07/20/pytest%E5%AE%9E%E6%88%98%EF%BC%9A%E4%BB%8E%E5%8D%95%E6%96%87%E4%BB%B6%E8%84%9A%E6%9C%AC%E5%88%B0%E5%8F%AF%E7%BB%B4%E6%8A%A4%E7%9A%84%E6%B5%8B%E8%AF%95%E5%B7%A5%E7%A8%8B/"/>
    <id>https://Scofieldsu.github.io/2022/07/20/pytest实战：从单文件脚本到可维护的测试工程/</id>
    <published>2022-07-20T21:00:00.000Z</published>
    <updated>2026-09-06T03:44:51.437Z</updated>
    
    <content type="html"><![CDATA[<p>很多团队的 pytest 用着像 unittest：一个文件塞几十个 test_xxx，互相依赖、顺序敏感、失败难排查。这篇文章讲怎么把 pytest 用成&quot;工程&quot;而不是&quot;脚本堆&quot;。</p><h2 id="一-目录结构约定大于配置"><a class="markdownIt-Anchor" href="#一-目录结构约定大于配置"></a> 一、目录结构：约定大于配置</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">tests/</span><br><span class="line">├── conftest.py          # 全局 fixture（驱动/数据库/环境）</span><br><span class="line">├── test_login.py</span><br><span class="line">├── test_order/</span><br><span class="line">│   ├── conftest.py      # 本目录专属 fixture（订单前置数据）</span><br><span class="line">│   ├── test_create.py</span><br><span class="line">│   └── test_pay.py</span><br><span class="line">└── support/</span><br><span class="line">    ├── client.py        # API 客户端封装</span><br><span class="line">    └── assertions.py    # 业务断言工具</span><br></pre></td></tr></table></figure><p>两条铁律：</p><ul><li><strong>fixture 就近声明</strong>：只在某目录用到的 fixture 放该目录的 <code>conftest.py</code>，全局共享的才放根目录；</li><li><strong>测试文件不 import 彼此</strong>：<code>test_a.py</code> 和 <code>test_b.py</code> 之间只允许通过 fixture 共享状态，直接 import 会制造隐式顺序依赖。</li></ul><h2 id="二-fixture-的正确打开方式"><a class="markdownIt-Anchor" href="#二-fixture-的正确打开方式"></a> 二、fixture 的正确打开方式</h2><h3 id="1-作用域分层"><a class="markdownIt-Anchor" href="#1-作用域分层"></a> 1. 作用域分层</h3><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@pytest.fixture(<span class="params">scope=<span class="string">&quot;session&quot;</span></span>)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">api_client</span>():</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;整个测试会话只建一次连接，最重的资源放 session 级&quot;&quot;&quot;</span></span><br><span class="line">    client = APIClient(base_url=ENV[<span class="string">&quot;base_url&quot;</span>])</span><br><span class="line">    client.login(<span class="string">&quot;admin&quot;</span>)</span><br><span class="line">    <span class="keyword">yield</span> client</span><br><span class="line">    client.close()</span><br><span class="line"></span><br><span class="line"><span class="meta">@pytest.fixture(<span class="params">scope=<span class="string">&quot;function&quot;</span></span>)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">fresh_order</span>(<span class="params">api_client</span>):</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;每个用例独立的订单数据，函数级保证隔离&quot;&quot;&quot;</span></span><br><span class="line">    order = api_client.create_order(sample_item())</span><br><span class="line">    <span class="keyword">yield</span> order</span><br><span class="line">    api_client.delete_order(order.<span class="built_in">id</span>)   <span class="comment"># teardown：清理，避免脏数据</span></span><br></pre></td></tr></table></figure><p><strong>经验</strong>：资源建立成本决定 scope。连接、驱动、登录态 → <code>session</code>；业务数据 → <code>function</code>。把 function 级的东西升成 session 级是数据污染的头号来源。</p><h3 id="2-参数化一份用例跑-n-组数据"><a class="markdownIt-Anchor" href="#2-参数化一份用例跑-n-组数据"></a> 2. 参数化：一份用例跑 N 组数据</h3><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@pytest.mark.parametrize(<span class="params"><span class="string">&quot;amount, expected&quot;</span>, [</span></span></span><br><span class="line"><span class="params"><span class="meta">    (<span class="params"><span class="number">100</span>, <span class="string">&quot;PAID&quot;</span></span>),</span></span></span><br><span class="line"><span class="params"><span class="meta">    (<span class="params"><span class="number">0</span>, <span class="string">&quot;REJECTED&quot;</span></span>),</span></span></span><br><span class="line"><span class="params"><span class="meta">    (<span class="params">-<span class="number">1</span>, <span class="string">&quot;REJECTED&quot;</span></span>),</span></span></span><br><span class="line"><span class="params"><span class="meta">    (<span class="params"><span class="number">99999999</span>, <span class="string">&quot;RISK_BLOCKED&quot;</span></span>),</span></span></span><br><span class="line"><span class="params"><span class="meta">]</span>)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test_pay_amount</span>(<span class="params">api_client, fresh_order, amount, expected</span>):</span></span><br><span class="line">    result = api_client.pay(fresh_order.<span class="built_in">id</span>, amount)</span><br><span class="line">    <span class="keyword">assert</span> result.status == expected</span><br></pre></td></tr></table></figure><p>参数化用例在报告里是<strong>独立行</strong>，哪组数据挂了看一眼就知道，比 if/else 分支清晰一个量级。</p><h2 id="三-标记mark与选择器让跑哪些可表达"><a class="markdownIt-Anchor" href="#三-标记mark与选择器让跑哪些可表达"></a> 三、标记（mark）与选择器：让&quot;跑哪些&quot;可表达</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">pytest.ini:</span><br><span class="line">[pytest]</span><br><span class="line">markers =</span><br><span class="line">    smoke: 冒烟用例（发版必跑）</span><br><span class="line">    api: 接口层</span><br><span class="line">    app: App 端 UI</span><br><span class="line">    slow: 单条超过 60s 的重型用例</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pytest -m <span class="string">&quot;smoke and not slow&quot;</span>     <span class="comment"># 冒烟，排除重型</span></span><br><span class="line">pytest tests/test_order -k <span class="string">&quot;pay&quot;</span>   <span class="comment"># 按名字模糊筛</span></span><br><span class="line">pytest --lf                        <span class="comment"># 只跑上次失败的（调试神器）</span></span><br></pre></td></tr></table></figure><p><strong>团队约定</strong>：CI 流水线分三档 profile——<code>smoke</code>（每次提交，5 分钟内）、<code>api</code>（每日构建，30 分钟）、<code>all</code>（发版前，2 小时）。用例没有标记 = 不许进主干。</p><h2 id="四-失败现场报告与产物"><a class="markdownIt-Anchor" href="#四-失败现场报告与产物"></a> 四、失败现场：报告与产物</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># conftest.py</span></span><br><span class="line"><span class="keyword">import</span> pytest</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">pytest_runtest_makereport</span>(<span class="params">item, call</span>):</span></span><br><span class="line">    <span class="keyword">if</span> call.when == <span class="string">&quot;call&quot;</span> <span class="keyword">and</span> call.excinfo <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">        report = call.report</span><br><span class="line">        <span class="comment"># 钩子里拿到当前用例的截图路径，附加到报告</span></span><br><span class="line">        report.extra.append(pytest_html.extrascreenshot(item.stash[<span class="string">&quot;screenshot&quot;</span>]))</span><br></pre></td></tr></table></figure><ul><li>用 <code>pytest-html</code> 出静态报告，<strong>失败用例自动带截图/请求响应 dump</strong>；</li><li>产物统一落 <code>var/results/&lt;run_id&gt;/</code>，报告头部写清：git sha、环境、分支——三个月后回看还能复现；</li><li>断言失败信息要带上下文：<code>assert result.status == &quot;PAID&quot;, f&quot;order=&#123;order.id&#125; resp=&#123;result.raw&#125;&quot;</code>，不要裸 assert。</li></ul><h2 id="五-常见反模式"><a class="markdownIt-Anchor" href="#五-常见反模式"></a> 五、常见反模式</h2><table><thead><tr><th>反模式</th><th>症状</th><th>解法</th></tr></thead><tbody><tr><td>用例间共享全局变量</td><td>单独跑过、一起跑挂</td><td>状态全部走 fixture</td></tr><tr><td>conftest 里写业务逻辑</td><td>改个接口要动 5 个 conftest</td><td>conftest 只建依赖，业务逻辑进 support/</td></tr><tr><td>用 <code>pytest.skip</code> 掩盖失败</td><td>跳过列表越来越长</td><td>skip 必须带 reason + issue 链接，限期清零</td></tr><tr><td>fixture 里吞异常</td><td>数据建失败却报&quot;断言失败&quot;</td><td>建依赖失败直接 raise，让失败发生在正确的地方</td></tr></tbody></table><h2 id="六-小结"><a class="markdownIt-Anchor" href="#六-小结"></a> 六、小结</h2><p>pytest 的威力在 <strong>fixture 依赖图 + 标记选择器</strong>：前者解决&quot;用例要什么&quot;，后者解决&quot;这次跑什么&quot;。把这两件事想清楚，测试工程就有了骨架，剩下的报告、CI、覆盖率都是插件级的事。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;很多团队的 pytest 用着像 unittest：一个文件塞几十个 test_xxx，互相依赖、顺序敏感、失败难排查。这篇文章讲怎么把 pytest 用成&amp;quot;工程&amp;quot;而不是&amp;quot;脚本堆&amp;quot;。&lt;/p&gt;
&lt;h2 id=&quot;一-目录结构约定大于配置&quot;
      
    
    </summary>
    
      <category term="后端开发" scheme="https://Scofieldsu.github.io/categories/%E5%90%8E%E7%AB%AF%E5%BC%80%E5%8F%91/"/>
    
    
      <category term="Python" scheme="https://Scofieldsu.github.io/tags/Python/"/>
    
      <category term="pytest" scheme="https://Scofieldsu.github.io/tags/pytest/"/>
    
      <category term="测试工程" scheme="https://Scofieldsu.github.io/tags/%E6%B5%8B%E8%AF%95%E5%B7%A5%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>App 自动化测试实战：定位策略与稳定性三板斧</title>
    <link href="https://Scofieldsu.github.io/2022/03/15/App%E8%87%AA%E5%8A%A8%E5%8C%96%E5%AE%9A%E4%BD%8D%E7%AD%96%E7%95%A5%E4%B8%8E%E7%A8%B3%E5%AE%9A%E6%80%A7%E4%B8%89%E6%9D%BF%E6%96%A7/"/>
    <id>https://Scofieldsu.github.io/2022/03/15/App自动化定位策略与稳定性三板斧/</id>
    <published>2022-03-15T20:30:00.000Z</published>
    <updated>2026-09-06T03:44:51.437Z</updated>
    
    <content type="html"><![CDATA[<p>做 App 自动化最头疼的不是&quot;能不能点中&quot;，而是&quot;为什么这次又点不中&quot;。这篇文章沉淀了 App 端自动化在<strong>元素定位</strong>和<strong>执行稳定性</strong>上最实用的经验，全部来自真实项目的踩坑。</p><h2 id="一-定位策略优先级从稳定到脆弱"><a class="markdownIt-Anchor" href="#一-定位策略优先级从稳定到脆弱"></a> 一、定位策略：优先级从稳定到脆弱</h2><p>App 控件树比 Web 复杂得多（原生 + H5 + 自绘 UI 混合），定位策略直接决定脚本寿命。推荐按以下优先级选择：</p><table><thead><tr><th>优先级</th><th>策略</th><th>说明</th><th>脆弱性</th></tr></thead><tbody><tr><td>1</td><td>resource-id / accessibility id</td><td>开发给控件起的稳定标识</td><td>低（需约定前缀规范）</td></tr><tr><td>2</td><td>content-desc（无障碍描述）</td><td>专为 AT 准备的描述</td><td>低</td></tr><tr><td>3</td><td>text 精确匹配</td><td>按钮/标签文案</td><td>中（多语言、动态文案会挂）</td></tr><tr><td>4</td><td>层级路径 + 索引</td><td><code>parent/child[2]</code></td><td>高（布局微调即失效）</td></tr><tr><td>5</td><td>XPath 全文档查询</td><td>万能但慢</td><td>高，且性能差</td></tr></tbody></table><p><strong>实践约定</strong>：和开发约定 resource-id 前缀（如 <code>com.app:id/btn_send</code>），UI 改版时前缀语义不变，脚本就能活下来。XPath 只用于&quot;实在没得选&quot;的兜底，且禁止写超过 5 层的路径。</p><h2 id="二-隐式等待告别固定-sleep"><a class="markdownIt-Anchor" href="#二-隐式等待告别固定-sleep"></a> 二、隐式等待：告别固定 sleep</h2><p>固定 <code>time.sleep(5)</code> 是稳定性的头号杀手：网络快时白白浪费时间，网络慢时照样超时。正确姿势是<strong>显式条件轮询</strong>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> appium.webdriver.wait <span class="keyword">import</span> WebDriverWait</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">wait_visible</span>(<span class="params">driver, locator, timeout=<span class="number">10</span></span>):</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;轮询等待元素可见，返回 WebElement；超时抛异常由上层决策&quot;&quot;&quot;</span></span><br><span class="line">    wait = WebDriverWait(driver, timeout, poll_frequency=<span class="number">0.5</span>)</span><br><span class="line">    <span class="keyword">return</span> wait.until(<span class="keyword">lambda</span> d: _first_visible(d, locator))</span><br></pre></td></tr></table></figure><p>要点：</p><ul><li><strong>轮询间隔 0.5s 起</strong>，间隔太小会打满 UIAutomator 的 dump 开销，反而拖慢整体；</li><li>等待条件分三级：<code>存在 → 可见 → 可点击</code>，点击前必须等到&quot;可点击&quot;；</li><li>超时后的动作要显式：截图 + 断言失败，而不是默默 continue。</li></ul><h2 id="三-异常恢复让失败可重试-可定位"><a class="markdownIt-Anchor" href="#三-异常恢复让失败可重试-可定位"></a> 三、异常恢复：让失败可重试、可定位</h2><h3 id="1-崩溃anr-自愈"><a class="markdownIt-Anchor" href="#1-崩溃anr-自愈"></a> 1. 崩溃/ANR 自愈</h3><p>App 崩溃后 driver 的 session 还活着但控件树已经没了，直接重试会连环失败。恢复顺序：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">ensure_app_alive</span>(<span class="params">driver, package, activity, max_relaunch=<span class="number">2</span></span>):</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(max_relaunch):</span><br><span class="line">        <span class="keyword">if</span> driver.current_package == package:</span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        driver.activate_app(<span class="string">f&quot;<span class="subst">&#123;package&#125;</span>/<span class="subst">&#123;activity&#125;</span>&quot;</span>)</span><br><span class="line">        wait_for_home(driver)</span><br><span class="line">    <span class="keyword">raise</span> AppCrashError(<span class="string">&quot;连续重启仍崩溃&quot;</span>)</span><br></pre></td></tr></table></figure><h3 id="2-弹窗拦截器"><a class="markdownIt-Anchor" href="#2-弹窗拦截器"></a> 2. 弹窗拦截器</h3><p>自动化最怕&quot;计划外弹窗&quot;（权限请求、升级提示、广告）。写一个统一的弹窗拦截，在每次关键操作前扫描：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">POPUP_RULES = [</span><br><span class="line">    ((<span class="string">&quot;确定&quot;</span>, <span class="string">&quot;允许&quot;</span>, <span class="string">&quot;忽略&quot;</span>), <span class="string">&quot;click&quot;</span>),   <span class="comment"># 常见确认类弹窗</span></span><br><span class="line">    ((<span class="string">&quot;以后再说&quot;</span>, <span class="string">&quot;取消&quot;</span>, <span class="string">&quot;残忍拒绝&quot;</span>), <span class="string">&quot;click&quot;</span>),  <span class="comment"># 升级/广告类</span></span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">dismiss_popups</span>(<span class="params">driver</span>):</span></span><br><span class="line">    <span class="keyword">for</span> texts, action <span class="keyword">in</span> POPUP_RULES:</span><br><span class="line">        <span class="keyword">for</span> t <span class="keyword">in</span> texts:</span><br><span class="line">            el = driver.find_element(AppiumBy.ACCESSIBILITY_ID, t)</span><br><span class="line">            <span class="keyword">if</span> el.is_displayed():</span><br><span class="line">                el.click()</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">False</span></span><br></pre></td></tr></table></figure><h3 id="3-断言分级"><a class="markdownIt-Anchor" href="#3-断言分级"></a> 3. 断言分级</h3><ul><li><strong>硬断言</strong>：核心流程节点（消息发出、订单创建），失败即用例失败；</li><li><strong>软断言</strong>：UI 细节（文案、颜色），失败只记录不中断，汇总后看趋势。</li></ul><h2 id="四-小结"><a class="markdownIt-Anchor" href="#四-小结"></a> 四、小结</h2><p>稳定性三板斧：<strong>定位走稳定标识、等待走条件轮询、失败走显式恢复</strong>。这三条做到位，App 自动化脚本的周通过率能从 60% 提到 95% 以上。剩下的 5% 基本是产品自身的 bug——那正是自动化测试存在的意义。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;做 App 自动化最头疼的不是&amp;quot;能不能点中&amp;quot;，而是&amp;quot;为什么这次又点不中&amp;quot;。这篇文章沉淀了 App 端自动化在&lt;strong&gt;元素定位&lt;/strong&gt;和&lt;strong&gt;执行稳定性&lt;/strong&gt;上最实用的经验，全部来自真实项目的踩坑
      
    
    </summary>
    
      <category term="App自动化测试" scheme="https://Scofieldsu.github.io/categories/App%E8%87%AA%E5%8A%A8%E5%8C%96%E6%B5%8B%E8%AF%95/"/>
    
    
      <category term="稳定性" scheme="https://Scofieldsu.github.io/tags/%E7%A8%B3%E5%AE%9A%E6%80%A7/"/>
    
      <category term="Appium" scheme="https://Scofieldsu.github.io/tags/Appium/"/>
    
      <category term="定位策略" scheme="https://Scofieldsu.github.io/tags/%E5%AE%9A%E4%BD%8D%E7%AD%96%E7%95%A5/"/>
    
  </entry>
  
  <entry>
    <title>59条编程语录</title>
    <link href="https://Scofieldsu.github.io/2022/01/18/59%E6%9D%A1%E7%BC%96%E7%A8%8B%E8%AF%AD%E5%BD%95/"/>
    <id>https://Scofieldsu.github.io/2022/01/18/59条编程语录/</id>
    <published>2022-01-18T20:56:40.000Z</published>
    <updated>2026-09-06T03:44:51.437Z</updated>
    
    <content type="html"><![CDATA[<h2 id="59条搞笑但却真实无比的编程语录"><a class="markdownIt-Anchor" href="#59条搞笑但却真实无比的编程语录"></a> 59条搞笑但却真实无比的编程语录</h2><blockquote><p>英文原文：59 Hilarious but True Programming Quotes for Software Developers</p></blockquote><p>收集了很多编程语录，这些语录涉及软件开发，代码维护，调试纠错，软件bug，系统设计、文档，代码质量，测试和软件开发团队管理等方面。</p><p>下面的这59条语录虽然很搞笑，但却真实无比。只有牛逼的程序员才能理解这些编程语句里的真正内涵。</p><hr /><h3 id="软件开发人员和程序员语录"><a class="markdownIt-Anchor" href="#软件开发人员和程序员语录"></a> 软件开发人员和程序员语录</h3><ol><li>A good programmer is someone who always looks both ways before crossing a one-way street.(Doug Linder)</li></ol><blockquote><p>好的程序员即使在过单行道时也总是会环顾两边。</p></blockquote><ol start="2"><li>Don’t worry if it doesn’t work right. If everything did, you’d be out of a job.(Mosher’s Law of Software Engineering)</li></ol><blockquote><p>不要担心它能否正常工作。如果一切正常，那么你就会失去工作。</p></blockquote><ol start="3"><li>The trouble with programmers is that you can never tell what a programmer is doing until it’s too late. (Seymour Cray)</li></ol><blockquote><p>程序员的烦恼是，你永远无法知道一个程序员在做什么，直到为时已晚。</p></blockquote><ol start="4"><li>Most of you are familiar with the virtues of a programmer. There are three, of course: laziness, impatience, and hubris. (Larry Wall)</li></ol><blockquote><p>程序员的美德：懒惰，没有耐心以及老子天下第一。</p></blockquote><ol start="5"><li>Always code as if the guy who ends up maintaining your code will be a violent psychopath who knows where you live. (Martin Golding)</li></ol><blockquote><p>写代码的时候总是想象维护你代码的家伙是一个知道你住在哪里的暴力精神病患者。</p></blockquote><ol start="6"><li>One man’s crappy software is another man’s full time job. (Jessica Gaston)</li></ol><blockquote><p>一个人写的烂软件将会给另一个人带来一份全职工作。</p></blockquote><ol start="7"><li>If builders built buildings the way programmers wrote programs, then the first woodpecker that came along wound destroy civilization. (Gerald Weinberg)</li></ol><blockquote><p>如果建筑工人用程序员写程序的方式建造建筑物，那么来的第一只啄木鸟（找bug）就将摧毁文明。</p></blockquote><ol start="8"><li>The most likely way for the world to be destroyed, most experts agree, is by accident. That’s where we come in; we’re computer professionals. We cause accidents. (Nathaniel Borenstein)</li></ol><blockquote><p>大多数专家认为，世界被破坏的最有可能的方式是，是因为偶然。那就是我们所要进入的领域：我们是计算机专家。我们制造偶然。</p></blockquote><ol start="9"><li>It’s a curious thing about our industry: not only do we not learn from our mistakes, we also don’t learn from our successes. (Keith Braithwaite)</li></ol><blockquote><p>这是我们这个行业的一件咄咄怪事：我们不仅不从错误中学习，我们也不从成功中学习。</p></blockquote><ol start="10"><li>Once a new technology starts rolling, if you’re not part of the steamroller, you’re part of the road. (Stewart Brand)</li></ol><blockquote><p>一旦一种新技术开始滚动碾压道路，如果你不能成为压路机的一部分，那么你就只能成为道路的一部分。</p></blockquote><ol start="11"><li>If at first you don’t succeed, call it version 1.0 (unknown)</li></ol><blockquote><p>如果第一次你没有成功，那么称之为1.0版，继续加油。</p></blockquote><ol start="12"><li>All programmers are playwrights and all computers are lousy actors. (Anonymous Hack Actor)</li></ol><blockquote><p>所有的程序员都剧作家，而所有计算机都是糟糕的演员。</p></blockquote><ol start="13"><li>The sooner you get behind in your work, the more time you have to catch up. (Anonymous Scheduler)</li></ol><blockquote><p>工作拉下得越早，赶上去所需要的时间越多。</p></blockquote><ol start="14"><li>When a programming language is created that allows programmers to program in simple English, it will be discovered that programmers cannot speak English. (Anonymous Linguist)</li></ol><blockquote><p>当创建一种编程语言允许程序员使用简单英语编程的时候，那么新问题又来了，你会发现程序员不说英语。</p></blockquote><ol start="15"><li>Why do we never have time to do it right, but always have time to do it over? (Anonymous)</li></ol><blockquote><p>为什么我们总是没有时间把事情做对，却有时间做完它？</p></blockquote><ol start="16"><li>Any fool can write code that a computer can understand. Good programmers write code that humans can understand.</li></ol><blockquote><p>傻瓜写计算机能理解的代码。优秀的程序员写人类能读懂的代码。</p></blockquote><ol start="17"><li>Any code of your own that you haven’t looked at for six or more months might as well have been written by someone else. (Eagleson’s law)</li></ol><blockquote><p>自己的代码六个月或更久不见，还不如别人写的代码。</p></blockquote><hr /><h3 id="编程软件开发语录"><a class="markdownIt-Anchor" href="#编程软件开发语录"></a> 编程/软件开发语录</h3><ol><li>Measuring programming progress by lines of code is like measuring aircraft building progress by weight. (Bill Gates)</li></ol><blockquote><p>通过代码行数衡量开发进程就好比用重量来衡量飞机制造进度。</p></blockquote><ol start="2"><li>Programming is like sex. One mistake and you have to support it for the rest of your life. (Michael Sinz)</li></ol><blockquote><p>编程就像性爱一样。一个错误就能绑住你的余生。</p></blockquote><ol start="3"><li>Walking on water and developing software from a specification are easy if both are frozen. (Edward V Berard)</li></ol><blockquote><p>在目的明确的基础上开发软件就像在结冰的水上行走一样简单。</p></blockquote><ol start="4"><li>The first 90 percent of the code accounts for the first 90 percent of the development time…The remaining 10 percent of the code accounts for the other 90 percent of the development time.(Tom Cargill)</li></ol><blockquote><p>开始90％的代码占开始90％的开发时间……剩下10％的代码还是占90％的开发时间。</p></blockquote><ol start="5"><li>Commenting your code is like cleaning your bathroom — you never want to do it, but it really does create a more pleasant experience for you and your guests. (Ryan Campbell)</li></ol><blockquote><p>注释代码就像是清洁浴室——尽管你满心不情愿，但它确实可以为你和你的客人创造一种更舒适的体验。</p></blockquote><ol start="6"><li>Programming today is a race between software engineers striving to build bigger and better idiot-proof programs, and the universe trying to produce bigger and better idiots. So far, the universe is winning. (Rick Cook)</li></ol><blockquote><p>当今的编程是软件工程师——努力建设更大多更好的傻瓜式程序，和上帝——试图产出更大多更好的傻瓜之间的竞争。到目前为止，世上帝是获胜者。</p></blockquote><ol start="7"><li>The hardest part of design … is keeping features out. (Donald Norman)</li></ol><blockquote><p>设计中最难的部分……是阻止功能。</p></blockquote><ol start="8"><li>In order to understand recursion, one must first understand recursion. (Anonymous)</li></ol><blockquote><p>要想了解递归，首先必须理解递归。</p></blockquote><ol start="9"><li>There are only two kinds of programming languages: those people always bitch about and those nobody uses. (Bjarne Stroustrup)</li></ol><blockquote><p>只有两种编程语言：一种是天天挨骂的，另一种是没人用的。</p></blockquote><ol start="10"><li>The best thing about a boolean is even if you are wrong, you are only off by a bit. (Anonymous)</li></ol><blockquote><p>关于布尔值最妙的一点是，就算你错了，你也只是错了这一处而已。</p></blockquote><ol start="11"><li>If Java had true garbage collection, most programs would delete themselves upon execution. (Robert Swell)</li></ol><blockquote><p>如果Java真的有垃圾收集，那么大多数程序会执行自我删除。</p></blockquote><ol start="12"><li>In theory, there is no difference between theory and practice. But, in practice, there is. (Jan L. A. van de Snepscheut)</li></ol><blockquote><p>理论上看，理论和实践之间是没有差别的。但是，在实践中，有。</p></blockquote><ol start="13"><li>Ready, fire, aim: the fast approach to software development. Ready, aim, aim, aim, aim: the slow approach to software development. (Anonymous)</li></ol><blockquote><p>准备，开火，瞄准：快速的软件开发方法。准备，瞄准，瞄准，瞄准，瞄准：缓慢的软件开发方法。</p></blockquote><ol start="14"><li>Programming is 10% science, 20% ingenuity, and 70% getting the ingenuity to work with the science. (Anonymous)</li></ol><blockquote><p>编程就是10％的科学，20％得的独创性，再加70％和这门科学的灵巧合作。</p></blockquote><ol start="15"><li>It is easier to measure something than to understand what you have measured. (Anonymous)</li></ol><blockquote><p>衡量比理解你衡量了什么容易。</p></blockquote><ol start="16"><li>Benchmarks don’t lie, but liars do benchmarks. (Anonymous)</li></ol><blockquote><p>基准不会撒谎，但制定基准的人会撒谎。</p></blockquote><ol start="17"><li>Better train people and risk they leave – than do nothing and risk they stay. (Anonymous)</li></ol><blockquote><p>就算是培训好的员工离开——也好过他们什么也不做却留下来。</p></blockquote><ol start="18"><li>The goal of Computer Science is to build something that will last at least until we’ve finished building it. (Anonymous)</li></ol><blockquote><p>计算机科学的目标是构建一个至少能持续到我们构建完成的东西。</p></blockquote><ol start="19"><li>Java is to JavaScript what Car is to Carpet. (Chris Heilmann)</li></ol><blockquote><p>Java之于JavaScript就像car之于carpet（地毯）。后者看上去像是前者的衍生物，实则是完全不同的两个东西。</p></blockquote><ol start="20"><li>It’s not at all important to get it right the first time. It’s vitally important to get it right the last time. (Andrew Hunt and David Thomas)</li></ol><blockquote><p>第一次是否正确完成一点也不重要。最后一次正确完成才至关重要。</p></blockquote><ol start="21"><li>Should array indices start at 0 or 1? My compromise of 0.5 was rejected without, I thought, proper consideration. (Stan Kelly-Bootle)</li></ol><blockquote><p>数组的下标一定要从0或1开始吗？我认为，我采取的中间值0.5没有经过适当考虑就被驳回了。</p></blockquote><ol start="22"><li>Programs must be written for people to read, and only incidentally for machines to execute. (Abelson / Sussman)</li></ol><blockquote><p>程序必须写得能供人阅读，机器执行只是附带。</p></blockquote><ol start="23"><li>Programming can be fun, so can cryptography; however they should not be combined. (Kreitzberg and Shneiderman)</li></ol><blockquote><p>编程可以很有趣，密码学也是如此；但是两者不应该搅和在一起。</p></blockquote><ol start="24"><li>Copy and paste is a design error. (David Parnas)</li></ol><blockquote><p>复制粘贴是一个设计错误。</p></blockquote><ol start="25"><li>Computers are good at following instructions, but not at reading your mind. (Donald Knuth)</li></ol><blockquote><p>计算机善于遵循指令，不善于读取你的想法。</p></blockquote><hr /><h3 id="软件调试语录"><a class="markdownIt-Anchor" href="#软件调试语录"></a> 软件调试语录</h3><ol><li>Deleted code is debugged code. (Jeff Sickel)</li></ol><blockquote><p>删除的代码是经过调试的代码。</p></blockquote><ol start="2"><li>If debugging is the process of removing software bugs, then programming must be the process of putting them in. (Edsger Dijkstra)</li></ol><blockquote><p>如果调试是消除软件bug的过程，那么编程就是产出bug的过程。</p></blockquote><ol start="3"><li>Debugging is twice as hard as writing the code in the first place. Therefore, if you write the code as cleverly as possible, you are–by definition–not smart enough to debug it.</li></ol><blockquote><p>调试一段程序的难度是写出这段程序的难度的两倍，因此，如果你的代码尽可能清楚的话，那么你就不用费力地调试它。</p></blockquote><ol start="4"><li>It’s hard enough to find an error in your code when you’re looking for it; it’s even harder when you’ve assumed your code is error-free. (Steve McConnel)</li></ol><blockquote><p>你的目的是找bug的时候，bug已经很难找了；更不要说当你认为自己的代码中没有错误的时候。</p></blockquote><hr /><h3 id="软件bug语录"><a class="markdownIt-Anchor" href="#软件bug语录"></a> 软件bug语录</h3><ol><li>It’s not a bug – it’s an undocumented feature. (Anonymous)</li></ol><blockquote><p>这不是bug——这是没有被证明的功能。</p></blockquote><ol start="2"><li>Without requirements or design, programming is the art of adding bugs to an empty text file. (Louis Srygley)</li></ol><blockquote><p>如果没有需求和设计，那么编程就是添加bug到空的文本文件的艺术。</p></blockquote><ol start="3"><li>Bad code isn’t bad, its just misunderstood. (Anonymous Code Behaviorist)</li></ol><blockquote><p>所谓坏的代码并不坏，它只是被误解了。</p></blockquote><ol start="4"><li>There are two ways to write error-free programs; only the third one works. (Alan J. Perlis)</li></ol><blockquote><p>写没有错误的程序有两种方式；但第三种才有效。</p></blockquote><ol start="5"><li>Beware of bugs in the above code; I have only proved it correct, not tried it. (Donald Knuth)</li></ol><blockquote><p>小心上面代码中的bug；我只证明了它是正确的，还没有尝试过。</p></blockquote><hr /><h3 id="软件终端产品语录"><a class="markdownIt-Anchor" href="#软件终端产品语录"></a> 软件/终端产品语录</h3><ol><li>Before software can be reusable it first has to be usable. (Ralph Johnson)</li></ol><blockquote><p>在软件可以被可重复使用前，它首先必须是可用的。</p></blockquote><ol start="2"><li>Software undergoes beta testing shortly before it’s released. Beta is Latin for “still doesn’t work”. (Anonymous)</li></ol><blockquote><p>软件在发布前要经过beta测试。beta即拉丁语“仍然无法正常工作”的意思。</p></blockquote><ol start="3"><li>The best performance improvement is the transition from the nonworking state to the working state. (J. Osterhout)</li></ol><blockquote><p>最佳性能改进是指从不可工作状态向工作状态的转变。</p></blockquote><ol start="4"><li>The cheapest, fastest, and most reliable components are those that aren’t there. (Gordon Bell)</li></ol><blockquote><p>最便宜，最快和最可靠的组件是不存在的。</p></blockquote><ol start="5"><li>I think Microsoft named .Net so it wouldn’t show up in a Unix directory listing. (Oktal)</li></ol><blockquote><p><a href="http://xn--siqv5luj359ai1csy4gjtf.Net">我认为微软命名.Net</a>，它就不会在Unix目录列表显示出来。</p></blockquote><ol start="6"><li>Software and cathedrals are much the same – first we build them, then we pray. (Sam Redwine)</li></ol><blockquote><p>软件和教堂非常相似——首先，我们建造它们，然后我们祈祷。</p></blockquote><ol start="7"><li>The software isn’t finished until the last user is dead. (Anonymous)</li></ol><blockquote><p>直到最后一个用户死去，软件才算完成。</p></blockquote><ol start="8"><li>Most software today is very much like an Egyptian pyramid with millions of bricks piled on top of each other, with no structural integrity, but just done by brute force and thousands of slaves. (Alan Kay)</li></ol><blockquote><p>现在的大多数软件非常像埃及金字塔，在彼此之间堆建了成千上万的砖块，缺乏结构完整性，只是靠蛮力和成千上万的奴隶完成。</p></blockquote>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;59条搞笑但却真实无比的编程语录&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#59条搞笑但却真实无比的编程语录&quot;&gt;&lt;/a&gt; 59条搞笑但却真实无比的编程语录&lt;/h2&gt;
&lt;blockquote&gt;
&lt;p&gt;英文原文：59 Hilarious
      
    
    </summary>
    
      <category term="编程" scheme="https://Scofieldsu.github.io/categories/%E7%BC%96%E7%A8%8B/"/>
    
    
  </entry>
  
  <entry>
    <title>Python测试框架</title>
    <link href="https://Scofieldsu.github.io/2021/12/08/Python%E6%B5%8B%E8%AF%95%E6%A1%86%E6%9E%B6/"/>
    <id>https://Scofieldsu.github.io/2021/12/08/Python测试框架/</id>
    <published>2021-12-08T21:13:45.000Z</published>
    <updated>2026-09-06T03:44:51.437Z</updated>
    
    <summary type="html">
    
    </summary>
    
      <category term="自动化测试" scheme="https://Scofieldsu.github.io/categories/%E8%87%AA%E5%8A%A8%E5%8C%96%E6%B5%8B%E8%AF%95/"/>
    
    
      <category term="自动化测试" scheme="https://Scofieldsu.github.io/tags/%E8%87%AA%E5%8A%A8%E5%8C%96%E6%B5%8B%E8%AF%95/"/>
    
      <category term="Python" scheme="https://Scofieldsu.github.io/tags/Python/"/>
    
  </entry>
  
</feed>
